# Changelog

All notable changes to **evo-crm-community** (umbrella) will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

This repository is the umbrella of the CRM Community family: it orchestrates the 7 submodules via Docker Compose. For per-service details, see the `CHANGELOG.md` inside each submodule.

## [Unreleased]

## [v1.1.0] - 2026-09-02

**The release that turns the family's permission model from "the admin bypasses everything" into record-scoped, fail-closed RBAC, and lets every AI and integration secret move into an encrypted credential vault — inline values stay as the designed fallback until the migration rakes run.** The window is **2026-07-15 → 2026-09-02 (49 days)** and **965 commits across the family**, measured per service from the pointer this repo carried at `v1.0.0` to the frozen head this release bumps to (frontend 347, CRM 335, auth 105, core 78, processor 48, flow 40, bot-runtime 12); the seven `from..to` ranges are listed under *Repository housekeeping* so every count is reproducible with `git rev-list --count`. **Unlike v1.0.0, this release ships database migrations: 22 of them, in three services — auth 9, CRM 5, core-service 8 (`000016`–`000023`) — including four forward-only permission revocations in auth, an irreversible SKU backfill in the CRM, and an irreversible model backfill in core that requires the migrate role to OWN `evo_core_agents`.** Read the upgrade notes before pulling the tag; several of these are one-way doors. `evo-flow-community` and `evo-bot-runtime` ship real deltas this time — no service in the family is an alignment-only re-tag.

Eight themes:

1. **Least-privilege `agent`, and the administrative gates moved server-side.** The default `agent` role is rebuilt as an attendance-only role: auth's data migrations revoke `conversations.read_all` (CRM-181), `conversations.delete` / `contacts.delete` / `pipeline_stages.delete` / `teams.create|update|delete` (CRM-182), `labels.delete` / `macros.delete` / `canned_responses.delete` / `message_templates.delete` (CRM-190) and `macros.create|update` / `message_templates.create|update` (CRM-70) from the existing system role — custom roles untouched, every `down` a deliberate no-op because re-granting would be an escalation relative to a fresh install. The catalog grows 322→333 keys / 49→51 resources: `ai_integration_credentials`, `pipeline_items.update` (CRM-178), standalone `macros.manage` / `message_templates.manage` / `teams.manage` registered in `STANDALONE_ACTIONS_BY_RESOURCE` so the coarse `write` group never confers them (CRM-70), `users.reset_password` (CRM-210), and `custom_attribute_definitions.read` for `agent` (CRM-166). Two grant migrations carry the new keys to installations that already exist, and they cut in opposite directions: `20260815120001` backfills `ai_integration_credentials.*` and `ai_api_keys.*` to `super_admin` and `account_owner` (`agent` deliberately excluded), while `20260818120001` grants the three standalone `.manage` keys to every non-`agent` role already holding the superseded create/update keys — which **widens a create-only role to full manage**, because `.manage` has no finer split. Enforcement followed: auth's `Api::V1::UsersController#check_authorization` runs BEFORE `fetch_user` and layers `users.manage` on `create`/`destroy`/`bulk_create`/`set_password` and any role-set change (EVO-2116/EVO-2026) — that gate previously lived only in the browser; a role update now genuinely replaces instead of accumulating, destroying every non-derived role of the target, so the role reported by `/api/v1/auth/validate` changes for anyone who had collected a second grant (CRM-496); the CRM's `InstallationConfigPolicy#manage?` dropped the `@user&.administrator? ||` term entirely and is now the sole gate of `/api/v1/admin/*`, denying with 403 instead of the 401 that logged the operator out (CRM-366); and the frontend moved `teams`, `macros` and `message_templates` routes from `read` to `manage`, put system roles behind a duplicate-as-custom-role path (EVO-2152), and removed the Macros "Execute" action (CRM-150).

2. **Pipelines become record-scoped, and archived pipelines stop being written to.** `PipelinePolicy::Scope#resolve` returns `Pipeline.accessible_by(user)` instead of `scope.all`, and `update?`, `destroy?`, `archive?`, `set_as_default?`, `dependents?` and the new `update_items?` all compose `accessible_record?` by re-running the same Scope, so a detail action can no longer disagree with the list (EVO-2204); service-token callers stay exempt. EVO-2222 implements the `team` visibility the form already offered (new `pipeline_teams` join table, `has_many :teams`, links dropped when visibility leaves `team`). Card writes moved off the manager-level `pipelines.update` onto `pipeline_items.update`, so an agent can move a card without reshaping the funnel (CRM-178, backfilled by auth migration `20260817120009` to every role that already held `pipelines.update`). EVO-2205 changed two contracts in the same area: `GET /api/v1/pipelines` now returns only ACTIVE pipelines unless the caller opts into inactive ones — every picker built on that endpoint sees a smaller list — and `DELETE /api/v1/pipelines/:id` blocks only on ACTIVE `pipeline_items`, with the refusal text changed from "Cannot delete pipeline with active conversations" to "Cannot delete pipeline with active items" (the error code is unchanged). Four cards close the archived-pipeline hole from both ends: archiving is now informed (`GET /pipelines/:id/dependents`, redacting form names the caller cannot read) and stamps the capture-form slug onto the contact so attribution survives a card delete (EVO-2200/EVO-2207); stage automations and the inactivity scheduler refuse to run on an archived pipeline (EVO-2201); automation rules can no longer push a conversation into one — the guard sits ahead of `execute_pipeline_assignment`, whose `destroy_all` would otherwise wipe every other membership, and the refusal is written to the rule's timeline instead of showing green (EVO-2202); and `pipeline_items` writes on an archived pipeline are rejected outright (EVO-2203). `PipelineStagesController` stopped swallowing malformed writes (CRM-254/CRM-255) and `AutomationRulesController` builds conditions by hand, closing a strong-params bypass that persisted arbitrary jsonb (CRM-298).

3. **AI and integration secrets move into an encrypted vault, with an ordered scope chain (EVO-2250).** Core-service creates `evo_core_integration_credentials` (migration `000019`, Fernet-encrypted with the same `ENCRYPTION_KEY` the registry and processor already share), a `kind` discriminator with a database CHECK that keeps the vault from becoming a token-refresh owner (`static` owns its value; `oauth` carries `value IS NULL` and points at `(owner_store, owner_ref)`, made unique by `000020`), plus `credential_refs` JSONB on `evo_core_custom_tools` and `evo_core_custom_mcp_servers` (`000021`) and `key_hint` / `scope` / `imported_from` / `base_url` on `evo_core_api_keys` (`000016`–`000018`, `000022`). The CRM resolves consumers through `Ai::CredentialResolver`, which returns an `Endpoint` pairing key with `base_url` so a key can never be sent to another provider's URL, and `Ai::ScopeChain` (`BASE_CHAIN %i[installation account]`, walked most-specific-first) exposes `insert_link(scope, before:)` so the enterprise overlay adds an `agency` link without reopening a resolver (EVO-1794). The processor resolves refs by id out of the vault for custom-tool headers, remote MCP headers, official MCP env vars, external agents and the Knowledge Nexus key, with the inline value as fallback. The frontend adds `/settings/ai-credentials` and `/settings/integration-credentials`, and retires the inline secret inputs per consumer off `GET /integration-credentials/migration-state` — a failing guard reads as NOT retired, the safe direction. The umbrella wires `EVO_AI_ENCRYPTION_KEY` onto the CRM web and Sidekiq services on all three deploy surfaces; it must be the same Fernet value core and processor read as `ENCRYPTION_KEY`. Migration is by idempotent, abort-on-drift rake pairs (`ai_credentials:migrate_dry_run|migrate`, `integration_credentials:migrate_dry_run|migrate`), and `Ai::MigrationState.legacy_fallback_active?` keeps the `OPENAI_API_SECRET` path alive until an install has actually moved (CRM-187).

4. **Fail-open closures, in four services (five closures).** Core-service's `EvoAuthService.CheckPermission` granted access on *every* failure, because `httpclient.DoJSON` wrapped transport errors and every non-2xx alike as `NetworkError` and that branch read as "endpoint not implemented" — an outage, a 5xx, a timeout, a 401 or a 403 therefore allowed every `RequirePermission` route; only a 404 now maps to `NotImplementedError`, and even that only with the new opt-in `EVO_AUTH_ALLOW_MISSING_PERMISSION_ENDPOINT=true` (CRM-461). In the same repo's enterprise build, the tenant middleware bound `app.current_tenant_id` to whatever `X-Evo-Tenant-Id` asked for, because `tenant.NewSQLAuthorizer` honours `EVO_LICENSING_PERMISSIVE_MEMBERSHIP` — the new `enforcedAuthorizer` resolves membership first and ignores the flag, reproducing the gem's `TenantPolicy` allow-set, and the plan `agents` limit is now enforced on the real write path (`POST /agents` and `POST /agents/import`, 422 `QUOTA_EXCEEDED`) instead of on a Rails controller the CRM community never defines (CRM-116). Auth's `Api::V1::ResourceActionsController` stopped returning `true` for unmapped actions, `bulk_update_permissions` returns 403 for any `system: true` role instead of answering 200 and being silently reverted by the next `db:seed`, and the new `RbacGrantReconciler` converges `super_admin` with the catalog on every boot (`rails rbac:reconcile_super_admin`, with a read-only `rbac:check_super_admin_drift` that exits non-zero on drift) (EVO-2062/EVO-2152). The processor's `validate_agent_api_key` no longer swallows a `SQLAlchemyError` into `{"valid": False}` — a dead pooled connection is now a retryable 503, not a non-retryable 401 that dropped the customer's message (EVO-2166). And the frontend's `permissionsService` stopped answering `[]` on a failed `/permissions` fetch, which reached the user as a blanket denial: it rethrows, the stale-cache fallback is bounded at one hour past expiry, and `PermissionsContext` gained a tri-state `FetchStatus` with a real load-failure panel (CRM-164/CRM-494/CRM-495).

5. **The AI turn stops failing silently: media forwarded end to end, failures classified, retries bounded.** Media needed both halves of the pair and both shipped: on the CRM side EVO-2179 makes `BotRuntime::DelegationService#build_message_event` emit `attachments: build_attachments` with TTL-signed media URLs, and on the runtime side EVO-2180 buffers those `attachments[]` across the debounce window in `bot_runtime:attach:<contact_id>:<conversation_id>` and forwards them as base64 A2A file parts whose field names match the processor's `extract_files_from_message` — 15 MiB per attachment, 20 MiB per call, mime resolved from `Content-Type` then the CRM-declared type then the extension, with `text/html` and `application/octet-stream` refused so a Rails error page never ships as an image. The buffer's 10-minute TTL is deliberately sized under the 15-minute lifetime the CRM signs those URLs with, so a stale entry never outlives its links. Fetching is gated by `MEDIA_HOST_ALLOWLIST`, read from the process environment only — an allowlist chosen by whoever sends the event constrains nobody — and re-checked on every redirect hop (EVO-2178); `BOT_RUNTIME_SECRET` became mandatory at boot, closing a path where any caller omitting `X-Bot-Runtime-Secret` was authenticated on `/events`. The processor call is retried on network errors and 429/500/502/503/504 with jittered exponential backoff under an overall ceiling (EVO-2167), and a failed turn now sends the customer one sentence through the postback URL instead of going quiet (CRM-236); `AI_CALL_TIMEOUT_SECONDS` went 30→90 in code, in `k8s/configmap.yaml`, and — the part that actually mattered — in every umbrella `.env`/compose file that was pinning it back to 30. On the processor side, provider failures stop collapsing into 500: `provider_errors.py` walks the `__cause__`/`__context__` chain and maps provider-anchored links to 429/-32010, 503/-32011, 502/-32014, 502/-32012 and 413/-32013, with every detail passed through `redact_secrets` and truncated at 600 chars (CRM-236, narrowed by CRM-424); a caller that hangs up now cancels the run and gets 499 instead of burning provider quota for another five minutes; non-audio attachments finally reach the model — the append to `file_parts` had been gated behind `if is_audio`, so an image was stored as an artifact and the agent answered "No content to process" (EVO-2181/EVO-2178); and the pipeline tool was fixed at both ends of the prompt (CRM-235/237/238): stage lines are rendered one per STAGE with a `stage_id` each, the instruction finally tells the model when to act, and the id precedence **inverted** — a `conversation_id`/`contact_id` extracted from the conversation context now WINS over whatever the model passed, a mismatch is logged as `[CRM-237] ignoring …`, and both fields are documented as "DO NOT SET". Google Calendar gained cancel/edit tools and a single-call slot search, at the cost of one behavior change: the per-day business-hours config is now read regardless of the top-level `business_hours['enabled']` flag, so an install that used that flag as a global off switch starts returning slots.

6. **Segments and journeys stop computing the wrong audience.** evo-flow's segment SQL filtered `label_added` / `label_removed` / `contact_deleted` reading `properties`, while the CRM emits `contact.label.added` / `contact.label.removed` / `contact.deleted` with the payload in `traits`: every Label segment computed zero members and a deleted contact kept counting. Both spellings now live in `contact-event-names.ts`, the Label condition matches `traits.labelId` OR `traits.labelName` so definitions saved by the old editor match without a backfill, and the deleted-contacts optimization no longer rewrites the CASE to `WHEN 1=0` on a cold cache (CRM-215). Every `whereProperties` condition now extracts with `JSONExtractString(if(JSONHas(p,'k'), p, traits), 'k')` — the old `properties`-only form made Equals/Contains never match and NotEquals/NotContains always true, a wrong audience rather than an empty one — and the duplicated, drifted operator switches in Performed, LastPerformed, `atomic-processor` and `batch-processor` all collapse into one shared builder (CRM-241). Segment-definition values are now escaped at every interpolation point (CRM-60), and `NotExists` on a custom attribute finally includes contacts that never triggered the change event. On the journey side, `WebhookTrigger` matched any `webhook.` prefix, so every SendGrid/Resend/SES deliverability callback started every journey holding a Webhook trigger — it now requires the exact `webhook.journey_trigger` (CRM-256/CRM-257) — and `JourneyTriggerProcessor` drops events with a blank `contactId` before any contact-scoped work, instead of opening Temporal workflows keyed on the empty string (CRM-271). Template messages deliver again after the EVO-1716 route cutover (CRM-209), CRM 422 refusal reasons reach the run instead of "Bad Request Exception" (EVO-2203) — with two error-string changes that anyone alerting on journey runs must absorb: the 404 text became contextual (`CRM Resource not found (${context.nodeType})`, previously hardcoded `(conversation)`), and a 422's top-level `message` is now overwritten with the CRM's reason, so callers that parsed the old generic text must read `getResponse().error.code` instead. Set Variable finally honors Increase/Decrease — and throws rather than writing a wrong number (EVO-1840). The frontend moved journeys off `apiEvoFlow` onto the CRM's new `/api/v1/journeys*` passthrough proxy (EVO-2191/EVO-2188), leaving campaigns as the only direct evo-flow consumer — which is why `VITE_EVOFLOW_API_URL` stays required in every deployment.

7. **WhatsApp: Baileys removed, connection state made honest, Meta signup in-page.** `Channel::Whatsapp::PROVIDERS` dropped `baileys` and the provider's services, handlers, helper and the `BAILEYS_PROVIDER_USE_INTERNAL_HOST_URL` switch were deleted. Connection truth was reworked around `provider_connection['credentials_verified_at']`, preserved across unrelated snapshots and discarded on close/disconnect, with `record_credential_probe!(:ok|:rejected|:inconclusive)` driving `authorization_error!`/`reauthorized!` (CRM-339) and a new hourly `Channels::Whatsapp::CredentialProbeSchedulerJob` on queue `low` re-asking whatsapp_cloud and notificame whether a token still works (CRM-469; 360dialog excluded because its probe re-registers the webhook). `DELETE /inboxes/:id/hub_connection` discards a Hub connection that never completed, row-locked against a racing `channel.connected` webhook (CRM-368) — the Hub channel is created before Meta's OAuth, so abandoning the flow used to leave an orphan inbox burning plan quota — and the frontend completes the whole connection in-page via `useFacebookSdk` plus `connect_info` / `whatsapp_connect` (CRM-314). A message the provider refuses before sending now ends `failed` with `content_attributes.external_error` instead of showing a checkmark (CRM-448) — which is also a **volume change downstream**: consumers of `message.failed`, EvoFlow automations and alerting included, start receiving events for a class of message that used to be silent. `POST /api/v1/inboxes/:id/set_agent_bot` with a present-but-unknown bot id now answers 404 instead of falling through to the unlink branch with a 200. Browser webm voice notes are transcoded to ogg/opus (CRM-112), Meta's handshake refuses a blank or mismatched `FB_VERIFY_TOKEN` (CRM-286), and `channelStatus.ts` gained a fifth verdict, `unmonitored`, for channel types the backend has no health support for, while `unknown` with a real `health_source` now derives `attention` rather than `active` (CRM-465).

8. **New ingress and catalog work: purchase webhooks, remote product import, and a wholesale model-catalogue refresh.** `POST /api/v1/webhooks/purchases/:provider` turns an approved sale into a contact plus a card on the pipeline's entry stage, with per-platform verifiers for virtu/hotmart/kiwify/cakto, an HMAC-pinned destination in the `d` query param (a platform's body signature cannot cover the query, so a hand-built URL fails closed with 401), a unique partial expression index on `(pipeline_id, purchase provider, purchase_id)` as the idempotency backstop, and a Rack::Attack ceiling because payment platforms burst redeliveries after an outage (CRM-320/CRM-483/CRM-493). `POST /api/v1/products/import_fetch` pulls a Shopify/WooCommerce catalog through SSRF-filtered connectors bounded at 25 page requests / 40s / 15s per request and returns items in the existing bulk-import shape without writing anything (EVO-1785); `Products::ImageIngestor` reports rejected images instead of failing silently (EVO-2226); and blank SKUs are normalized to NULL and backfilled, since the partial unique index had been treating `''` as a real value and rejecting the second product without a SKU (CRM-289). The pinned model list was replaced wholesale in the frontend — the GPT-4/4o/4.1/4.5/3.5 range, Gemini 1.5/2.0 and the 2.5 previews, Claude 3.x, the Together Llama-2 entries, Bedrock `amazon.titan-text-express-v1` and `anthropic.claude-3-*`, Vertex `*-bison`, Perplexity `r1-1776`/`sonar-reasoning` are gone, replaced by a current set with Bedrock ids prefixed `global.` only where Bedrock offers global routing, and four guard specs now hold the line (CRM-424/442/463/482). Core-service narrowed its Gemini listing to current general-purpose chat models by dynamic metadata filter rather than a fixed allowlist, and the agent-repair default moved off `gpt-4.1-nano` — which OpenAI retires on 2026-10-23 and which `sanitizeAgent` was stamping into customer data merely because someone opened the listing — onto the LiteLLM-prefixed `openai/gpt-5.6-luna`, with migration `000023` moving the rows the old repair already wrote (CRM-464/CRM-480, mirrored by the processor's `DEFAULT_REPAIR_MODEL`).

### Submodules updated

- **evo-auth-service-community** v1.1.0 — 105 commits (`2d9884f..851073d`). **(headline)** The default `agent` role is rebuilt as a least-privilege attendance role and endpoint-level RBAC is tightened across the users/roles APIs, carried to already-bootstrapped installations by **nine new data migrations — four of them forward-only permission revocations**. `agent` loses `conversations.read_all` (`20260817120003`, CRM-181), `conversations.delete` / `contacts.delete` / `pipeline_stages.delete` / `teams.create|update|delete` (`20260817120005`, CRM-182), the shared-asset deletes `labels.delete` / `macros.delete` / `canned_responses.delete` / `message_templates.delete` (`20260817120007`, CRM-190) and `macros.create|update` / `message_templates.create|update` (`20260818120003`, CRM-70); each of those four has a deliberate no-op `down`. The other five are grants: `20260815120001` backfills `ai_integration_credentials.{read,create,update,delete}` and `ai_api_keys.{…}` to `super_admin` and `account_owner` (idempotent, `agent` deliberately excluded — only CUSTOM roles need those keys granted by hand); `20260817120009` gives `pipeline_items.update` to `agent` and to every role already holding `pipelines.update`; `20260818120001` gives `macros.manage` / `message_templates.manage` / `teams.manage` to every non-`agent` role already holding the superseded create/update keys, deliberately excluding `teams.read` because it is in `User::BASIC_READ_PERMISSIONS`; `20260818120005` gives `custom_attribute_definitions.read` to `agent`; `20260822010001` gives `users.reset_password` to `account_owner` and `super_admin`. The permission catalog grows 322→333 keys / 49→51 resources: `ai_integration_credentials` for the credential vault (EVO-2250), `pipeline_items.update` (CRM-178), standalone `macros.manage` / `message_templates.manage` / `teams.manage` (CRM-70), `users.reset_password` (CRM-210) and `custom_attribute_definitions.read` for `agent` (CRM-166). `users.manage` is now enforced server-side on `create`/`destroy`/`bulk_create`/`set_password` and on any role-set change, and authorization runs before the target is fetched, so an unauthorized caller gets 403 rather than a 404 that leaked id existence (EVO-2116/EVO-2026). New `POST /api/v1/users/:id/set_password` lets an admin set another user's password, refusing self-service and a non-super_admin targeting a super_admin, then revoking every unrevoked `Doorkeeper::AccessToken` for the target and logging actor/target/count at WARN (CRM-210). The installation-owner invariant is now self-healing: `RbacGrantReconciler` converges `super_admin` with the catalog on every boot via `docker-entrypoint.sh`, `rails rbac:check_super_admin_drift` exits non-zero on drift, and `bulk_update_permissions` returns 403 for any `system: true` role instead of answering 200 and being reverted by the next seed (EVO-2062/EVO-2152). `Licensing::RetryPolicy` adds bounded 30s→30min backoff with jitter and honors the server's `Retry-After` as a floor, and heartbeats gained a generation-token chain so stale chains die instead of accumulating (CRM-394/CRM-491). `FORCE_SSL` (default `true`) now gates `config.force_ssl`, `assume_ssl` and Doorkeeper's `force_ssl_in_redirect_uri`; the entrypoint probes with `rails db:version` and only calls `db:create` when the database is unreachable (CRM-216); `ApplicationMailer` stops requesting SMTP AUTH when the user name is blank, and `EmailConfirmationPosture.log_boot_posture!` shouts LOCKOUT when `REQUIRE_EMAIL_CONFIRMATION` forces the confirmation barrier with no resolvable mailer — `MAILER_TYPE=resend|bms` suppresses that warning (EVO-2146); and `GET /users` moved to `Users::FilterService` with whitelisted attributes and bind-parameterised values (EVO-1947). ⚠️ **Populate `inbox_members` for every agent that needs conversation visibility BEFORE migration `20260817120003` runs** — an agent with no membership row sees an empty conversation list afterwards, there is no auto-recovery (the CRM's `inbox_members.empty? -> Inbox.all` degrade was removed), and the revocation is not reversible; the migration logs the blast radius on `up`. ⚠️ The four agent revocations are forward-only: rolling back does not restore the grants, and `db:seed` will not either. ⚠️ `20260818120001` is additive and reversible but **widens privilege**: a custom role that could only CREATE macros, message templates or teams now MANAGES them, because `.manage` has no finer split. ⚠️ `down` on `20260822010001` removes `users.reset_password` from EVERY role, not just the two it granted — repair with `db:seed`. ⚠️ Callers holding only `users.create`/`users.delete` now get 403; a non-GET/HEAD request to an unmapped `ResourceActions` action now 403s instead of being allowed. ⚠️ A role update now revokes EVERY non-derived role of the target (`replaceable_roles_of`), not just `system: false` ones (CRM-496): a user who had accumulated a second grant sees the role reported by `/api/v1/auth/validate` and by the users list change to the one the last grant assigned. ⚠️ A `HeartbeatJob` enqueued before this release carries no generation argument, fails `chain_alive?` and exits without rescheduling — intentional cleanup of accumulated chains, but the first heartbeat after upgrade comes from the newly scheduled chain, not the old job. ⚠️ `POST /setup/bootstrap` can answer 201 with `{"status":"degraded"}` (extension point `after_bootstrap` 1.0.0 → 1.1.0) — a client matching on `status == 'ok'` will read a successful install as a failure. ⚠️ Set `FORCE_SSL=false` on any installation terminating TLS at an external proxy or reached over plain HTTP. ⚠️ If the app role is NOCREATEDB (as `evo_app` is by design), create the database out-of-band before first boot — the `RUN_MIGRATIONS != "false"` branch now probes with `rails db:version` and only calls `db:create` when the database is unreachable. ⚠️ Any external consumer asserting on catalog size must move 322 → 333 keys and 49 → 51 resources.
- **evo-ai-crm-community** v1.1.0 — 335 commits (`ad7523c..aa3d408`), **five migrations** (schema.rb `2026_07_05_120000` → `2026_08_26_120000`): `20260725130000` creates the `pipeline_teams` join table (additive, reversible, sub-second), `20260727120000` adds the capture-form lead indexes CONCURRENTLY (long-running, self-heals a leftover INVALID index on retry), `20260729140000` adds the nullable `agent_bots.credential_id` uuid with no FK on purpose (the vault table is owned by core-service), `20260824120000` backfills blank product SKUs to NULL (IRREVERSIBLE), and `20260826120000` adds the UNIQUE purchase-identity index CONCURRENTLY (can fail on existing duplicates). **(headline)** The permission model turns from "administrator bypasses everything" into record-scoped RBAC: `PipelinePolicy::Scope#resolve` returns `accessible_by(user)` and every write predicate composes `accessible_record?`, so an administrator no longer sees another user's private pipeline and a detail action cannot disagree with the list (EVO-2204); `pipeline_teams` implements the `team` visibility the form already offered (EVO-2222); and `InstallationConfigPolicy#manage?` dropped `administrator?` entirely, making `installation_configs.manage` the sole gate of `/api/v1/admin/*` and denying with 403 instead of a session-killing 401 (CRM-366). Macros and message templates split use from manage (CRM-70), macro lookups are scoped so another user's personal macro answers 404 instead of leaking (CRM-195), and `POST /macros/:id/execute` stops returning a green 200 when nothing ran (CRM-152/CRM-54). AI and integration secrets move into a vault with an ordered scope chain — `Ai::CredentialResolver` returns an `Endpoint` pairing key with `base_url`, `Ai::ScopeChain.insert_link` lets the enterprise overlay add an `agency` link, `agent_bots.credential_id` points into the core-owned tables with no FK on purpose, and two abort-on-drift rake pairs perform the move while `Ai::MigrationState` keeps the legacy `OPENAI_API_SECRET` path alive until an install has actually migrated (EVO-2250/EVO-1794/CRM-187). New unauthenticated ingress `POST /api/v1/webhooks/purchases/:provider` turns an approved sale into a contact plus a pipeline card, destination pinned by HMAC and idempotency enforced by a unique partial index (CRM-320/CRM-483/CRM-493). The Baileys WhatsApp provider is deleted and connection truth reworked around `credentials_verified_at` plus an hourly credential probe (CRM-339/CRM-469); archived pipelines are closed off from stage automations, automation rules and item writes (EVO-2201/2202/2203); `POST /api/v1/products/import_fetch` fetches a Shopify/WooCommerce catalog behind SSRF filtering and outbound bounds (EVO-1785); `BotRuntime::DelegationService#build_message_event` now emits `attachments: build_attachments` with TTL-signed media URLs, the producer half the bot-runtime's media forwarding consumes (EVO-2179); the `permission_resolver` extension point lands with contract 2.0.0 → 2.1.0 and folds the runtime scope into the per-request permission cache key (EVO-2130/EVO-2156); and `/health/ready` now returns 503 with `"schema":"pending_migrations"` while any migration — engine migrations included — is pending (EVO-2252). ⚠️ **Two of the five migrations use `disable_ddl_transaction!` and build indexes CONCURRENTLY over `contacts` and `pipeline_items`** — do not wrap the migrate step in an outer transaction, and expect minutes on a large database. ⚠️ `20260826120000` builds a UNIQUE index: if any pipeline already holds two cards with the same purchase provider + id, the build fails and leaves an INVALID index that must be dropped BY HAND before retrying (unlike the capture-form migration, this one has no self-healing drop) — check for duplicates first. ⚠️ `20260824120000` is irreversible and rewrites every `products`/`product_variants` row whose SKU is empty or whitespace. ⚠️ Point container and load-balancer probes at `/health/ready`, never `/health/live`. ⚠️ Existing inboxes with `provider='baileys'` have no working provider service after upgrade and must be migrated first. ⚠️ `GET /api/v1/pipelines` now returns only ACTIVE pipelines unless the caller opts into inactive ones (every picker sees a smaller list), and `DELETE /api/v1/pipelines/:id` blocks only on ACTIVE `pipeline_items` with the message changed to "Cannot delete pipeline with active items" — the error code is unchanged (EVO-2205). ⚠️ `GET /conversations/unread_count` was renamed to `unanswered_count` with different semantics; contact notes moved to the `{ success, data, meta }` envelope; `EvoAuthService#check_account_permission` was removed; and `POST /api/v1/inboxes/:id/set_agent_bot` with a present-but-unknown bot id now answers 404 instead of 200. ⚠️ A WhatsApp message the provider refuses before sending now ends `failed` with `content_attributes.external_error` instead of showing a checkmark (CRM-448) — consumers of `message.failed`, including EvoFlow automations and alerting, will see a volume increase for a class of message that used to be silent. ⚠️ `EvoFlow::PublishEventWorker#perform` gained a third positional `tenant_id` (nil-defaulted, so already-enqueued two-argument jobs still run) — hand-enqueued jobs should pass it, and `EvoFlowTenantPassthrough` now forwards `X-Evo-Tenant-Id` and `Authorization` on the journeys and segments proxies, without which a multi-tenant evo-flow answers 401 `TENANT_REQUIRED` even to the proxy. ⚠️ The auth catalog must carry `macros.manage`, `message_templates.manage` and `pipeline_items.update`, and `installation_configs.manage` must be granted to whoever needs the admin panel. ⚠️ `FRONTEND_URL` is newly load-bearing: `Webhooks::PurchaseWebhookUrl` refuses to mint with `host_not_configured` when it is unset. Mint registration URLs with `rake "evo_purchase_webhook:url[provider,evo_tenant,pipeline_id,product]"` (or from the pipeline screen) — a hand-built URL fails closed with 401 — and note the target pipeline must have at least one stage. ⚠️ Run the four credential rakes after deploying (`ai_credentials:migrate_dry_run|migrate`, `integration_credentials:migrate_dry_run|migrate`) — the core-service must be migrated first. ⚠️ New hourly job `whatsapp_credential_probe_scheduler_job` on queue `low` — the scheduler must reload `schedule.yml` and something must drain that queue. ⚠️ `ACTIVE_STORAGE_SERVICE` now resolves ENV-first; installations hit by the stale-`installation_configs` regression must fix the row or delete it, and blobs written to the container disk during the affected window are lost (CRM-111).
- **evo-ai-frontend-community** v1.1.0 — 347 commits (`c8bc64e..20ba0e6`), 86 merged PRs, no migrations (there is no `db/migrate` in this repo). **(headline)** The AI-credential vault lands as two new Settings screens — `/settings/ai-credentials` (`ai_api_keys.read`) and `/settings/integration-credentials` (`ai_integration_credentials.read`) — while the inline secret inputs retire per consumer behind `GET /integration-credentials/migration-state`, where a network failure, a missing permission or an older backend all read as NOT retired (EVO-2250/CRM-187/CRM-413). A failed permission load is no longer served as a denial: `permissionsService` rethrows instead of answering `[]`, the stale-cache fallback is bounded at one hour past the 30-minute expiry, each fetch carries a request id so an older response cannot overwrite a newer one, and `PermissionsContext` replaces its two boolean flags with a tri-state `FetchStatus` plus a real load-failure panel (CRM-164/CRM-494/CRM-495). Route-level RBAC tightened from `read` to `manage` on Teams, Macros and Message templates, Custom Attributes now requires one of the administrative create/update/delete keys (CRM-166), system roles became read-only with a duplicate-as-custom-role path (EVO-2152), and `useCredentialScopePermissions` encodes the rule that an installation-scope control needs both the verb on the resource and `installation_configs.manage`, gated per verb. Journeys stop calling evo-flow directly and go through the CRM proxy (EVO-2191); the sidebar badge follows the `unread_count` → `unanswered_count` rename (EVO-1963); Meta embedded signup completes in-page with orphan cleanup on abandon (CRM-314/CRM-368); `channelStatus.ts` gains the `unmonitored` verdict and stops reading `unknown` as active (CRM-465); a new `useContactUpdatedReconciler` reconciles `contact.updated` frames against REST before they reach the store, and `ChatActionCableConnector` was reading the payload from the wrong key (CRM-318). The agents workspace was rebuilt around horizontal tabs (`GeneralTab.tsx` and `AgentEditSidebar.tsx` deleted and split into focused panels), the CRM-68 journeys epic removed the auto-persist-on-load effect from six action panels and centralised node writes in one `BaseFlowCanvas` writer, and the pinned model catalogue was replaced wholesale with four lifecycle guard specs (CRM-424/442/463/482). ⚠️ Roles holding only `read` on `teams`, `macros` or `message_templates` lose those screens — review grants before rolling out. ⚠️ **`VITE_EVOFLOW_API_URL` stays required in every deployment.** Journeys moved to the CRM proxy, but campaigns remain the only direct consumer of `src/services/core/apiEvoFlow.ts`, and the variable must stay set until the CRM exposes an `/api/v1/campaigns` proxy — do not delete it. ⚠️ This build requires a co-deployed CRM exposing the journeys proxy, `unanswered_count`, `ai_credentials/migration_state`, the purchase-webhook config routes, `products/import_fetch` and the three Hub endpoints; a core-service exposing `/integration-credentials`; and an auth exposing `POST /users/:id/set_password`. Older backends degrade to broken screens, not fallbacks. ⚠️ **Redaction lists must move together across three repos:** `vaultRetirement.ts::SAFE_HEADER_NAMES` mirrors `safeHeaderNames` in core-service's Go `secretmerge` package, which the processor's `_SAFE_HEADER_NAMES` logging allowlist also mirrors. If the backend's list changes and this one does not, a redacted header renders as an editable empty box and tidying it away DESTROYS the stored secret. `src/constants/aiProviders.ts::isOpenAICompatible` and `resolveCredential` are likewise browser-side mirrors of core-service's `IsOpenAICompatible` and the Ruby `Ai::CredentialResolver`. ⚠️ Agents pinned to a retired model id keep the stored value but no longer see it in the picker — audit saved agents if the install runs Vertex, Bedrock or Perplexity, which have no listing endpoint. ⚠️ An empty live model listing is now authoritative and no longer falls back to the pinned list. ⚠️ `getAccessToken()` gives localStorage precedence over the in-memory copy, so two tabs on the same origin can no longer hold two different sessions. ⚠️ The Meta signup loads `connect.facebook.net` at runtime — a CSP or egress policy blocking that host breaks in-page WhatsApp connect. ⚠️ CI is new here: `npm run lint` now requires `eslint-suppressions.json`, and a fork that renames files must re-key the baseline.
- **evo-ai-processor-community** v1.1.0 — 48 commits (`5769ec6..a8e1bd4`), **zero migrations** (`migrations/` is untouched). **(headline)** Agent execution stops lying about why it failed: `provider_errors.py` walks the exception chain and, only for provider-anchored links, maps failures to 429/-32010, 503/-32011, 502/-32014, 502/-32012 and 413/-32013 instead of a blanket 500, with every detail passed through `redact_secrets` (`?key=AIza…`, `Bearer …`, `sk-…`) and truncated at 600 chars (CRM-236, matcher narrowed by CRM-424 after the loose variant started reporting our own KeyErrors as provider faults). A run whose caller hung up is now cancelled and answered with 499 rather than burning provider quota for another five minutes — detection awaits the ASGI `receive()` because `request.is_disconnected()` answers False forever once uvicorn pauses reading (CRM-236). Integration credentials resolve by id out of `evo_core_integration_credentials` for custom-tool headers, remote MCP headers, official MCP env vars, external agents and the Knowledge Nexus key, with the inline value as fallback, and two real leaks were closed alongside: `mcp_context` no longer copies an agent's MCP env vars into the processor's own `os.environ` (they were never unset, so one agent's token reached every MCP subprocess spawned afterwards, across tenants in the enterprise build), and header logging moved from a denylist to a `_SAFE_HEADER_NAMES` allowlist mirroring the Go `secretmerge` package (EVO-2250). Agent API-key validation raises instead of swallowing, so a DB blip is a retryable 503 rather than a terminal 401 (EVO-2166). The pipeline tool was fixed at both ends of the prompt: `_format_pipeline_rules_for_prompt` renders one line per STAGE off `rule["stages"]` and emits a `stage_id` on each (the old code read `stageName`/`stageId` off the rule itself, a shape the config does not carry, so the model could not call the tool), `_pipeline_tool_instruction` finally tells the model when to act, and the id precedence inverted (CRM-235/CRM-237/CRM-238). Google Calendar gained `cancel_calendar_event` and `edit_calendar_event`, all four tools now honour the UI-selected calendar, credentials are read unsanitized from `evo_core_agent_integrations` so the token refresh works, and slot search prefetches the whole window in ONE API call — the per-slot pattern took ~40s and blew past the bot-runtime's timeout, so the answer was never delivered (EVO-2163/2164/2165/2169/2170/2171). Attachments finally reach the model: the append to `file_parts` was gated behind `if is_audio`, and now runs for every file, before `save_artifact`, bounded by an ADK-shaped mime allowlist and 15 MiB/20 MiB ceilings (EVO-2181/EVO-2178). Custom tool names are sanitized to `^[a-zA-Z0-9_-]+$` at 64 chars, because a name with a space or an accent made the provider reject the whole turn (CRM-499). ⚠️ **The pipeline manipulation tool now IGNORES `conversation_id`/`contact_id` supplied by the model whenever the conversation context provides them** — the context wins, a mismatch is logged as `[CRM-237] ignoring …`, the docstring and schema say "DO NOT SET", and the required-field errors became "No conversation in context: this tool only runs inside a conversation." Any prompt or caller that relied on the model overriding the context stops working, silently from the model's point of view. ⚠️ **Calendar: the per-day business-hours config is now read regardless of the top-level `business_hours['enabled']` flag.** An installation that used that flag as a global off switch for scheduling will START RETURNING SLOTS after upgrade — turn scheduling off per day instead. ⚠️ Nothing to migrate here, but the schema this service reads DID move: deploy the core-service version shipping `credential_refs` and `evo_core_integration_credentials` first, or a SELECT against a missing column breaks MCP/custom-tool loading. ⚠️ **MCP key naming is a one-way coupling:** `mcp_service` now reads `environments` first and only tolerates `envs` for legacy entries. An older core-service that persists only `envs` still works with this processor; a processor OLDER than this one against a core-service writing `environments` does not. Do not run the processor behind core-service on that key. ⚠️ `A2A_CANCEL_ON_DISCONNECT` defaults to **true** — a caller that gives up now kills the run; set it to false if a deployment intentionally lets long runs finish. ⚠️ Set `POSTGRES_CONNECTION_STRING` wherever the processor runs if Google Calendar is in use. ⚠️ Non-audio attachments are now forwarded inline, raising per-turn payload size and provider token cost on conversations that previously sent only text. ⚠️ MCP stdio servers no longer inherit an agent's env vars through `os.environ` — a server working by accident because another agent leaked its variables will stop. ⚠️ Custom tools with non-conforming names are now exposed to the LLM under a different, sanitized name; prompts naming such a tool literally must be updated. ⚠️ The default model stamped on a malformed agent changed from `gpt-4.1-nano` to `openai/gpt-5.6-luna` — it mirrors core-service's `defaultRepairModel` and the two must be bumped together. ⚠️ **Carried regression:** the new CI excludes `tests/unit/test_exception_handlers.py`, because `generic_exception_handler` was removed and is no longer registered in `src/main.py`; that handler kept internal class names out of 500 bodies (EVO-972) and its absence ships in 1.1.0 needing its own card.
- **evo-ai-core-service-community** v1.1.0 — 78 commits (`729d691..c6f7ba3`), **eight migrations (`000016`–`000023`)**; v1.0.0 shipped none, so this upgrade requires a real migrate step. `000016` adds `key_hint`, `000017` adds `scope` with a CHECK and an index, `000018` adds `imported_from` with a partial unique index, `000019` creates the vault table `evo_core_integration_credentials`, `000020` adds the partial unique index on `(owner_store, owner_ref)` that the OAuth listing sync upserts on, `000021` adds `credential_refs` JSONB to `evo_core_custom_tools` and `evo_core_custom_mcp_servers`, `000022` adds `base_url` — all routine and additive — and `000023` is a data backfill that is irreversible and requires table ownership. **(headline)** The AI credential surface was rebuilt around a new encrypted integration-credential vault (Fernet-encrypted with the same `ENCRYPTION_KEY` the registry and processor share, gated on the new `ai_integration_credentials` resource), while the registry gained `key_hint`, `scope`, `imported_from` and `base_url` and two silent no-ops were closed by moving off GORM struct `Updates`: the Activate/Deactivate toggle answered 200 and changed nothing, and `base_url` had been sent by the UI since story 1.1 while existing in neither request, model nor schema, so every typed endpoint was discarded (EVO-2250/CRM-186). Two fail-open holes closed: `EvoAuthService.CheckPermission` granted access on every failure because `DoJSON` wrapped transport errors and non-2xx statuses alike, so an outage or a 403 allowed every gated route — only a 404 now maps to "not implemented", and only behind the new opt-in env (CRM-461) — and in the enterprise build the tenant middleware bound `app.current_tenant_id` without proving membership, since `tenant.NewSQLAuthorizer` honours `EVO_LICENSING_PERMISSIVE_MEMBERSHIP`; the new `enforcedAuthorizer` resolves membership first and ignores the flag. The plan `agents` quota is now enforced on the real write path (`POST /agents` and `POST /agents/import`, 422 `QUOTA_EXCEEDED`), where the only previous gate was a Rails `before_action` on a controller the CRM community does not define and which the SPA bypasses anyway (CRM-116). `reconstructCustomConfigurations` stopped persisting its expansion on every read, which had frozen a copy of the tool or MCP server inside the agent config so catalog edits never reached it (EVO-2126). Two stateless test-before-save endpoints landed for the wizards, re-gated from `read` to `create` in review since `read` would have handed every read-only user an arbitrary server-side HTTP fetcher (EVO-1738/EVO-1739). ⚠️ **`PUT /agents/:id` now MERGES the config instead of replacing it (CRM-305).** It had rebuilt the config from the request alone, so a one-toggle edit wiped `custom_tool_ids`, `mcp_servers`, `tools`, the segmentation settings and `inactivity_actions` and answered 200. The new contract: a key the request sends wins, **a key the request OMITS is preserved**, and clearing is explicit — `null` for a scalar, `[]` for a list. Any client that relied on omission to drop keys will silently stop dropping them. ⚠️ **Migration `000023` is an irreversible data backfill and requires the migrate role to OWN `evo_core_agents`** — the table is FORCE ROW LEVEL SECURITY, so the migration lifts FORCE for its own transaction; a non-owner role fails there by design, which aborts the migrate-core initContainer, stalls every deployment gated on it, and leaves golang-migrate at "Dirty database version 23" until forced. Count `WHERE model = 'gpt-4.1-nano'` before upgrading; the rewritten count is emitted with `RAISE LOG` to the Postgres server log, not to migrate output. ⚠️ Permission checks now fail closed — expect 500s where you previously got silent success if evo-auth is flaky, and verify evo-auth serves the permission endpoint before upgrading (`EVO_AUTH_ALLOW_MISSING_PERMISSION_ENDPOINT=true` covers only the 404 case and is temporary). ⚠️ Grant `ai_integration_credentials.{read,create,update,delete}` and `installation_configs.manage` in the auth catalog as part of the upgrade, not after. ⚠️ Enterprise deployments: populate `evo_enterprise_tenant_memberships` before upgrading — callers relying on `EVO_LICENSING_PERMISSIVE_MEMBERSHIP` start taking 403 — and check each tenant's `limits->>'agents'` against its current agent count, since a tenant already over its limit is refused its next create and a canceled subscription now imposes its plan's limit instead of meaning unlimited. ⚠️ `DELETE /agents/apikeys/:id` is a hard delete answering 404 for an absent key; `ApiKeyResponse` no longer returns `key`; header values are redacted by allowlist. ⚠️ `DELETE /integration-credentials/:id` answers **409 with `error.details.consumers`** when a custom tool, MCP server, agent, agent integration or bot still references the credential (and 409 when an oauth row's connection is still live); the consumer guard fails closed, so a store that cannot be read refuses the delete. ⚠️ SQLSTATE 42703/42P01 responses now APPEND the Postgres message text (schema identifiers only, never row data) to "Undefined column"/"Undefined table" — any client string-matching the old exact message breaks. ⚠️ Internal Go API: `agent_integration.InitModule(db, router)` became `InitModule(db, router, encryptionKey)` — relevant only to forks that call it. ⚠️ **`POST /custom-mcp-servers/test-connection` delegates the real MCP handshake to the processor's own `/custom-mcp-servers/test-connection` (EVO-2139), with tenant-header propagation: without a co-deployed processor at that version, the MCP wizard's Test button fails.** `referenced_by` also reads the CRM-owned `agent_bots` table directly from the shared database — a deliberate boundary decision, but it means the CRM's schema is a hard dependency of the delete guard. ⚠️ Rolling back `000019` DROPs the vault table and destroys every stored integration secret.
- **evo-flow-community** v1.1.0 — 40 commits (`ddc5753..1734849`), **zero migrations**, no Postgres/TypeORM file added and no ClickHouse DDL changed; a plain image bump with no config surface change. **(headline)** Segment membership and journey triggering were corrected end to end. The segment ClickHouse SQL filtered `label_added` / `label_removed` / `contact_deleted` reading `properties`, while the CRM emits `contact.label.added` / `contact.label.removed` / `contact.deleted` with the payload in `traits` — every Label segment computed zero members and a deleted contact kept counting; both spellings now live in one `contact-event-names.ts`, the Label condition matches `traits.labelId` OR `traits.labelName` so definitions written by the old editor match without a backfill, and the deleted-contacts optimization no longer rewrites the CASE to `WHEN 1=0` on a cold cache (CRM-215). Every event-property filter now extracts with `JSONExtractString(if(JSONHas(p,'k'), p, traits), 'k')`: the old `properties`-only form made Equals/Contains never match and NotEquals/NotContains always true — a wrong audience for a campaign, not an empty one — and the duplicated, drifted operator switches in Performed, LastPerformed, `atomic-processor` and `batch-processor` all delegate to one shared builder (CRM-241). Segment-definition values are escaped at every interpolation point and `SegmentQueryExecutionService` fails closed on an unmapped operator (CRM-60). `WebhookTrigger` matched any `webhook.` prefix, so every SendGrid/Resend/SES deliverability callback started every journey holding a Webhook trigger; it now requires the exact `webhook.journey_trigger` and deliberately does not read `eventName` off the node config, because the editor copies it onto every trigger node and never clears it on type change (CRM-256/CRM-257). `JourneyTriggerProcessor` drops events with a blank `contactId` before any contact-scoped work, where they previously opened Temporal workflows keyed on the empty string (CRM-271). Set Variable finally honours Increase/Decrease and throws rather than writing a wrong number (EVO-1840); template messages deliver again through the flat `message_templates` endpoint (CRM-209); and CRM 422 refusal reasons reach the journey run instead of "Bad Request Exception" (EVO-2203). ⚠️ **Re-verify campaign and journey audience sizes BEFORE the first mass send after upgrade** — several silent-wrong-audience bugs are fixed at once, so segment counts move in both directions on the first recompute. ⚠️ Journeys that were in practice being started by deliverability callbacks will stop starting; per the shipped documentation no producer currently publishes `webhook.journey_trigger`, so the handler matches nothing on the bus by design. ⚠️ **Journey-run error strings changed — re-check anything alerting on them.** `executeRequest`'s 404 text went from the hardcoded `CRM Resource not found (conversation)` to `CRM Resource not found (${context.nodeType})`, and on a 422 the exception's top-level `message` is now overwritten with the CRM's own reason (`CRM Validation error: <code>: <reason>` on the node path, keeping the load-bearing `CRM Validation error` prefix that suppresses retries) — callers that parsed the old generic message must read `getResponse().error.code` instead. ⚠️ **This version and the CRM must move together on message templates.** It calls only the flat `GET /api/v1/message_templates?inbox_id=…&active=true&per_page=-1` route (EVO-1716 or later): against an OLDER CRM that serves only the inbox-nested route, template-mode send-message nodes 404 and skip the send silently — and the reverse is equally true, a CRM at or after EVO-1716 breaks an evo-flow older than this version the same way. ⚠️ Expect a ClickHouse load increase on segment recompute when the deleted-contacts cache is cold and during the 15s bypass window after each ingested `contact.deleted`. ⚠️ `JourneysService.processWebhookTrigger` and its private `sanitizeHeaders` were removed (no callers, no route removed).
- **evo-bot-runtime** v1.1.0 — 12 commits (`040a486..7ec7db4`), no migrations and no schema of any kind (state is Redis-only). **(headline)** The runtime now forwards inbound media to the AI Processor as base64 A2A file parts behind a mandatory host allowlist, retries transient processor failures, and answers the customer with a notice instead of going silent when the AI call fails. `MessageEvent` gained `attachments[]`, buffered across the debounce window in the new key namespace `bot_runtime:attach:<contact_id>:<conversation_id>` (10-minute TTL, cleared with the text buffer), and `buildFileParts` emits parts whose field names match the processor's `extract_files_from_message` — bounded at 15 MiB per attachment and 20 MiB per call (~27 MiB after base64), with mime resolved from `Content-Type`, then the CRM-declared type, then the URL extension, and `text/html` / `application/xhtml+xml` / `application/octet-stream` refused so a Rails error page never ships as an image (EVO-2180). `checkMediaURL` rejects any non-http(s) scheme and any host absent from `MEDIA_HOST_ALLOWLIST`, re-running on every redirect hop so an authorized host cannot 302 the fetch onto an internal address; the allowlist moved out of the event payload and into the process environment, because one chosen by whoever sends the event constrains nobody (EVO-2178). The processor call retries network errors and 429/500/502/503/504 with jittered exponential backoff under an overall ceiling, marshalling the body once so every retry is byte-identical (EVO-2167), and `sendAIFailureNotice` dispatches one sentence through the postback URL under a dedicated 30s context on both the timeout and the generic error branch (CRM-236). ⚠️ **Media forwarding is INERT against a CRM older than the matching release.** The runtime never discovers media on its own: it forwards only what the CRM's `BotRuntime::DelegationService#build_message_event` puts in `attachments[]`, which is the producer half shipped as EVO-2179. Deploy this runtime against an older CRM and text keeps working while every attachment simply never arrives. The buffer's 10-minute TTL is deliberately sized under the 15-minute lifetime the CRM signs those media URLs with, so a stale entry never outlives its links. ⚠️ **Set `BOT_RUNTIME_SECRET` before rolling the new image** — it moved from `os.Getenv` to `mustGetEnv`, so an installation that never set it (and was therefore authenticating every caller that omitted `X-Bot-Runtime-Secret` on `/events`) will crash-loop on `config.Load`. ⚠️ `MEDIA_HOST_ALLOWLIST` is fail-closed: unset means no attachment is ever fetched, logged as `pipeline.ai.attachment.blocked_url` and otherwise silent. Set it to the host that signs attachment URLs — `ACTIVE_STORAGE_URL` when set, else `BACKEND_URL` — plus the storage host in redirect mode or the CDN host, no scheme, no port. ⚠️ Apply `k8s/configmap.yaml` (`AI_CALL_TIMEOUT_SECONDS` now `"90"`, new `MEDIA_HOST_ALLOWLIST` key) together with `k8s/deployment.yaml`; an old ConfigMap still pinning `"30"` overrides the new code default. ⚠️ The timeout is now per ATTEMPT: with the default 2 retries the worst-case turn is roughly 6 minutes — size upstream timeouts accordingly, and make sure the processor's ingress accepts bodies up to ~27 MiB, since a 413 is non-retryable and would cost the customer the text reply too. ⚠️ AI failures are no longer silent; set `AI_FAILURE_NOTICE=""` to restore the old behaviour. ⚠️ Go API changes for out-of-tree consumers: `DebounceEngine.Start/Reset` take an extra `[]model.Attachment`, `PipelineRepository` gained `AppendAttachments`/`GetAttachments`, and `NewAIAdapter` went from `(timeoutSecs)` to `(timeoutSecs, maxRetries, retryBaseMs)`.

### Non-family submodule pointers

- **evolution-api** is bumped `dd552c7` → `e273b90` (**12 commits**) in this window and **carries behavior change**, even though it keeps its own version scheme and is not part of the `v1.1.0` tag set: EvoHub link-existing-webhook (EVO-2098), a webhook secret rewrite, and a Docker build fix (EVO-2097 / patches dir). Treat it as a real service upgrade when planning the deploy, not as a traceability-only pointer.
- **evolution-go** (`706c9a4`) and **evo-nexus** (`fe15fd5`) are **unchanged** in this window. They keep their own version schemes, are not part of the `v1.1.0` tag set, and are recorded here for traceability only.

### Notes for upgrading an existing PROD

- ⚠️ **This release ships 22 database migrations across three services — auth 9, CRM 5, core-service 8 (`000016`–`000023`). v1.0.0 shipped none, so a plain image redeploy is not enough.** Migration order: **auth first**, because it owns the `users` table that the CRM's migrations reference, **then the CRM**. Core-service's migrations are independent of that pair but must be applied **before** the CRM's credential rake tasks are run and before the processor's vault path can work — the processor reads `credential_refs` and `evo_core_integration_credentials`, ships no migration for either, and a SELECT against a missing column breaks MCP/custom-tool loading. The frontend, evo-flow and bot-runtime have no migrations at all.
- ⚠️ **The 22 migrations, enumerated.**
  **auth (9)** — `20260815120001` backfills `ai_integration_credentials.*` and `ai_api_keys.*` to `super_admin` and `account_owner` (additive, idempotent, reversible; `agent` deliberately excluded); `20260817120003` revokes `conversations.read_all` from `agent` (**destructive, forward-only**); `20260817120005` revokes `conversations.delete` / `contacts.delete` / `pipeline_stages.delete` / `teams.create|update|delete` (**destructive, forward-only**); `20260817120007` revokes `labels.delete` / `macros.delete` / `canned_responses.delete` / `message_templates.delete` (**destructive, forward-only**); `20260817120009` grants `pipeline_items.update` to `agent` and to every role already holding `pipelines.update` (additive, reversible); `20260818120001` grants `macros.manage` / `message_templates.manage` / `teams.manage` (additive and reversible, but it **widens privilege** — see below); `20260818120003` revokes `macros.create|update` / `message_templates.create|update` (**destructive, forward-only**); `20260818120005` grants `custom_attribute_definitions.read` to `agent` (additive, reversible); `20260822010001` grants `users.reset_password` to `account_owner` and `super_admin` (routine on `up`, **lossy on `down`**).
  **CRM (5)** — `20260725130000` creates `pipeline_teams` (additive, reversible, sub-second); `20260727120000` builds the capture-form GIN and lead-slug indexes **CONCURRENTLY** (long-running, non-destructive, self-heals a leftover INVALID index on retry); `20260729140000` adds the nullable `agent_bots.credential_id` uuid, deliberately without a FK because the vault table belongs to core-service (additive, reversible); `20260824120000` normalizes blank product SKUs to NULL (**irreversible data backfill**); `20260826120000` builds the UNIQUE purchase-identity index **CONCURRENTLY** (**can fail on existing duplicates**).
  **core-service (8)** — `000016` `key_hint`, `000017` `scope` + CHECK + index, `000018` `imported_from` + partial UNIQUE, `000020` the partial UNIQUE on `(owner_store, owner_ref)` (built on a table created one migration earlier, so empty), `000021` `credential_refs` JSONB on custom tools and MCP servers, `000022` `base_url` — all routine and additive. The two that are not: `000019` creates the vault table, and **rolling it back DROPs that table and destroys every stored integration secret**; `000023` rewrites `evo_core_agents` off the retired model id and is **irreversible AND requires table ownership**.
- ⚠️ **auth `20260817120003` — populate `inbox_members` FIRST.** Revoking `conversations.read_all` from the `agent` role leaves an agent with zero inbox memberships looking at an empty conversation and inbox list, with no auto-recovery, because the CRM's `inbox_members.empty? -> Inbox.all` degrade was removed in the same window. The migration logs the blast radius on `up` (`CRM-181: N agent-role user(s) have ZERO inbox memberships…`) — read that line in the deploy log.
- ⚠️ **Four auth migrations are forward-only.** `20260817120003`, `20260817120005`, `20260817120007` and `20260818120003` all have a deliberate no-op `down`; rolling back the release does not restore the grants, and `db:seed` will not either because the seed no longer lists the keys. Restoring them is an explicit admin action. Separately, `down` on `20260822010001` is lossy — it removes `users.reset_password` from EVERY role, repairable only with `db:seed`.
- ⚠️ **auth `20260818120001` silently WIDENS privilege on existing custom roles.** It grants `macros.manage`, `message_templates.manage` and `teams.manage` to `super_admin`/`account_owner` and to every non-`agent` role already holding the superseded write keys (macros/message templates: `create`/`update`; teams: `create`/`update`/`delete`, deliberately NOT `teams.read`, which lives in `User::BASIC_READ_PERMISSIONS`). Because `.manage` has no finer split, a role that could only CREATE now MANAGES. In a release whose pitch is least privilege, audit your custom roles after this migration runs.
- ⚠️ **CRM `20260826120000` builds a UNIQUE index CONCURRENTLY.** If the database already holds two cards in one pipeline with the same purchase provider + `purchase_id`, the build fails and leaves an INVALID index that an operator must drop by hand before retrying — this one has no self-healing drop. Check for duplicates before upgrading. Both CONCURRENTLY migrations (`20260727120000` and this one) use `disable_ddl_transaction!`: do not wrap the migrate step in an outer transaction.
- ⚠️ **CRM `20260824120000` is irreversible** — a table-wide UPDATE normalizing blank SKUs to NULL on `products` and `product_variants`. Rows whose SKU was genuinely the empty string cannot be recovered from it. Take the usual backup.
- ⚠️ **core `000023` is irreversible AND requires table ownership.** It rewrites `evo_core_agents` rows off the retired `gpt-4.1-nano` onto `openai/gpt-5.6-luna`; because the table is FORCE ROW LEVEL SECURITY it lifts FORCE for its own transaction, so a migrate role that does not own the table fails there — loudly, by design (proven against a staging role that is NOSUPERUSER NOBYPASSRLS). That failure aborts the migrate-core initContainer, stalls every deployment gated on it, and leaves golang-migrate at "Dirty database version 23" until forced. Take `SELECT count(*) FROM evo_core_agents WHERE model = 'gpt-4.1-nano'` first. Rolling back `000019` DROPs the vault table and destroys every stored integration secret — do not roll back after credentials exist.
- ⚠️ **RBAC grants must be in place before the family goes up.** In the auth catalog: `macros.manage`, `message_templates.manage`, `teams.manage`, `pipeline_items.update`, `ai_integration_credentials.{read,create,update,delete}`, `ai_api_keys.*`, `users.reset_password` and `installation_configs.manage`. The two credential resources are **backfilled automatically to `super_admin` and `account_owner` by auth migration `20260815120001`** — only CUSTOM roles need them granted by hand, and `agent` is deliberately excluded. Everything else fails closed — the CRM's macro/template management and kanban card writes 403, core-service answers 403 on the whole vault and on every installation-scope write, and the two new frontend Settings screens are unreachable. `installation_configs.manage` is now the only thing standing between a user and `/api/v1/admin/*`: `administrator?` no longer bypasses it, so an `account_owner` that could reach installation settings before will get 403.
- ⚠️ **Permission checks fail closed in core-service (CRM-461).** Any evo-auth failure — outage, timeout, 5xx, 401, 403, or a 404 without the new opt-in — now denies with 500 instead of granting. Verify evo-auth actually serves the permission endpoint before upgrading; `EVO_AUTH_ALLOW_MISSING_PERMISSION_ENDPOINT=true` covers only the 404 case and is a temporary escape hatch, not a setting. Monitor the permission-check path during and after the upgrade.
- ⚠️ **Enterprise deployments: two new gates in core-service.** Populate `evo_enterprise_tenant_memberships` before upgrading — the new `enforcedAuthorizer` resolves membership before binding `app.current_tenant_id` and ignores `EVO_LICENSING_PERMISSIVE_MEMBERSHIP`, so callers who relied on that flag start taking 403. And check each tenant's `evo_enterprise_tenant_plans.limits->>'agents'` against its current `COUNT(*)` on `evo_core_agents`: the quota now enforces at agent create and bulk import (422 `QUOTA_EXCEEDED`), an absent/blank/non-integer limit means unlimited, `0` blocks everything, and a canceled subscription now imposes its plan's limit where it previously meant unlimited.
- ⚠️ **New and newly load-bearing env vars, by service.** **auth:** `FORCE_SSL` (new, default `true` — set `false` where TLS terminates at an external proxy or services talk plain HTTP); `MAILER_TYPE` (now read by `EmailConfirmationPosture.alternative_mailer_configured?` — `resend`/`bms` suppress the new boot LOCKOUT warning); `REQUIRE_EMAIL_CONFIRMATION` (semantics unchanged, but it now triggers a boot LOCKOUT warning when it forces the confirmation barrier with no resolvable mailer — and that warning is a documented FALSE ALARM when SMTP lives only in `installation_configs` and the database was down at boot); `RUN_MIGRATIONS` (unchanged variable, rewritten branch: it now probes with `rails db:version` and only calls `db:create` when the database is unreachable — if the app role is NOCREATEDB, as `evo_app` is by design, create the database out-of-band before first boot); and `SMTP_AUTHENTICATION` is now ignored when the resolved SMTP user name is blank. **CRM:** `RATE_LIMIT_PURCHASE_WEBHOOK` (60/min), `PRODUCT_IMPORT_FETCH_RATE_LIMIT` (10/min/IP), `EVOLUTION_HUB_API_URL` / `EVOLUTION_HUB_FRONTEND_URL`, the `installation_configs` secrets `PURCHASE_WEBHOOK_SECRET_<PROVIDER>`, `PURCHASE_WEBHOOK_SECRET_DESTINATION` (required for Hotmart/Kiwify/Cakto) and `BMS_API_URL`, plus **`FRONTEND_URL`, newly load-bearing** — `Webhooks::PurchaseWebhookUrl` refuses to mint with `host_not_configured` while it is unset. **core:** `EVO_AUTH_ALLOW_MISSING_PERMISSION_ENDPOINT`. **processor:** `A2A_CANCEL_ON_DISCONNECT` (defaults **true**) and `A2A_DISCONNECT_POLL_SECONDS`. **frontend:** `VITE_EVOFLOW_API_URL` is unchanged and **stays required** — journeys moved to the CRM proxy but campaigns are still a direct evo-flow consumer, so do not delete it until the CRM exposes an `/api/v1/campaigns` proxy. **bot-runtime:** `MEDIA_HOST_ALLOWLIST`, `AI_CALL_MAX_RETRIES`, `AI_CALL_RETRY_BASE_MS`, `AI_FAILURE_NOTICE`. **Umbrella:** `EVO_AI_ENCRYPTION_KEY` and `MEDIA_HOST_ALLOWLIST`. **Removed:** `BAILEYS_PROVIDER_USE_INTERNAL_HOST_URL`, with the provider.
- ⚠️ **Purchase-webhook enablement checklist (CRM).** Set `PURCHASE_WEBHOOK_SECRET_<PROVIDER>` in `installation_configs` for each platform you enable, `PURCHASE_WEBHOOK_SECRET_DESTINATION` for the public-credential platforms (Hotmart/Kiwify/Cakto), and `FRONTEND_URL`. Mint the registration URL with `rake "evo_purchase_webhook:url[provider,evo_tenant,pipeline_id,product]"` or from the pipeline screen — hand-built URLs fail closed with 401 because the `d` MAC pins the destination — and note that **the target pipeline must have at least one stage**, or minting refuses with `pipeline_without_stages`.
- ⚠️ **`EVO_AI_ENCRYPTION_KEY` must equal the `ENCRYPTION_KEY` core-service and processor already use.** It is one Fernet key across three services; generating a fresh one for the CRM makes previously stored AI credentials undecryptable. Compose users get it automatically via `${ENCRYPTION_KEY}`; **swarm operators must paste it into two services by hand**, and must likewise fill the blank `MEDIA_HOST_ALLOWLIST=` — the swarm file ships both empty by convention, and deploying it unfilled leaves AI credentials unstorable and media silently undelivered. Neither fails loudly.
- ⚠️ **`AI_CALL_TIMEOUT_SECONDS`: change it in your own `.env`.** The bot-runtime raised its code default 30 → 90 for CRM-236, but every shipped file set `30` explicitly, which overrides the default — the service-side fix was a no-op on every real stack. A fresh install copying the new `.env.example` is fine; an existing `.env` keeps the stale 30 and reproduces the original timeout on tool-using turns.
- ⚠️ **Media forwarding needs BOTH halves — CRM and bot-runtime must move together.** The producer is the CRM's `BotRuntime::DelegationService#build_message_event`, which emits `attachments[]` with TTL-signed URLs (EVO-2179); the consumer is the bot-runtime's buffer-and-forward (EVO-2180). A new bot-runtime against an older CRM forwards nothing — text keeps working, attachments simply never arrive, with no error. The runtime's 10-minute attachment buffer TTL is deliberately sized under the CRM's 15-minute signed-URL lifetime, so a buffered entry never outlives its links; and `MEDIA_HOST_ALLOWLIST` must name the host that signs those URLs (`ACTIVE_STORAGE_URL` when set, otherwise `BACKEND_URL`), a two-service coupling with no runtime cross-check.
- ⚠️ **Multi-tenant deployments: the CRM ↔ evo-flow tenant plumbing must be present on both sides.** `EvoFlowTenantPassthrough` forwards the caller's `X-Evo-Tenant-Id` and `Authorization` on the journeys and segments proxies, and `EvoFlow::PublishEventWorker` carries a `tenant_id` captured at enqueue time as `X-Evo-Tenant-Id`. Without these a multi-tenant evo-flow answers **401 `TENANT_REQUIRED`** even to the proxy (a single-tenant evo-flow sends neither header and is unaffected). Related breaking change: `EvoFlow::PublishEventWorker#perform` gained a **third positional `tenant_id`** — it defaults to nil so jobs already enqueued with two arguments still run, but anything enqueuing by hand should pass it.
- ⚠️ **evo-flow ↔ CRM message templates break in BOTH directions.** evo-flow at this version calls only the flat `GET /api/v1/message_templates?inbox_id=…` route: against a CRM older than EVO-1716 (nested route only), template-mode send-message nodes 404 and skip the send silently; and a CRM at or after EVO-1716 breaks an evo-flow older than this version the same way. The two must move together (CRM-209).
- ⚠️ **core-service ↔ processor, two version couplings.** The MCP wizard's **Test button** on `POST /custom-mcp-servers/test-connection` delegates the real handshake to the processor's own endpoint (EVO-2139) — without a processor at that version the button fails. And MCP agent-entry key naming is **asymmetric**: the processor now reads `environments` first and only tolerates `envs` for legacy entries, so an older core-service persisting only `envs` still works, but a processor older than this release against a core-service writing `environments` does not. Do not stage the processor behind core-service on that key.
- ⚠️ **Redaction lists are a three-repo trio with a secret-destroying failure mode.** The frontend's `vaultRetirement.ts::SAFE_HEADER_NAMES`, core-service's Go `secretmerge.safeHeaderNames` and the processor's `_SAFE_HEADER_NAMES` logging allowlist must move together. If a backend's list changes and the frontend's does not, a header that is in fact redacted renders as an editable EMPTY box — and tidying that empty box away destroys the stored secret. The same applies to `aiProviders.ts::isOpenAICompatible` (mirroring core-service's `IsOpenAICompatible`) and to the browser-side mirror of the Ruby `Ai::CredentialResolver` ordering.
- ⚠️ **Health probes: point at `/health/ready`, not `/health/live`.** Readiness now answers 503 with `"schema":"pending_migrations"` while any migration — including an engine's — is pending, and a boot on a fresh database can migrate for several minutes. A deployment whose probe accepted the old unconditional 200 will now hold the container out of rotation until migrations complete; that is the intended behavior (EVO-2252).
- ⚠️ **Baileys inboxes must be migrated to another provider before or during upgrade.** `'baileys'` is gone from `Channel::Whatsapp::PROVIDERS` and there is no code path left for a persisted inbox on it.
- ⚠️ **Renamed and reshaped API contracts.** CRM: `GET /conversations/unread_count` → `unanswered_count`, with different semantics (assigned to me + open + `waiting_since` present + not archived); contact notes moved to the `{ success, data, meta }` envelope on all five actions; `EvoAuthService#check_account_permission` was removed; `GET /api/v1/pipelines` returns only ACTIVE pipelines unless the caller opts in, and `DELETE /api/v1/pipelines/:id` now refuses with "Cannot delete pipeline with active items" (code unchanged, EVO-2205); `POST /api/v1/inboxes/:id/set_agent_bot` answers 404 for a present-but-unknown bot id instead of falling through to the unlink branch with 200; and `POST /setup/bootstrap` can answer `{"status":"degraded"}`. core-service: `PUT /agents/:id` merges instead of replacing (an omitted key is preserved; clear with `null` or `[]`), `DELETE /integration-credentials/:id` answers 409 with `error.details.consumers` while still referenced, and SQLSTATE 42703/42P01 responses now append the Postgres message text, breaking exact string matching. processor: A2A error responses no longer always carry 500/-32603. Update external integrations before the cut.
- ⚠️ **Automation and alerting volume rises on `message.failed`.** A WhatsApp message the provider refuses before sending now terminates as `failed` with `content_attributes.external_error` instead of sitting at `sent` with a checkmark (CRM-448). EvoFlow automations and alerting subscribed to `message.failed` start receiving events for a class of message that used to be silent — size the alert thresholds before the cut, not after.
- ⚠️ **Prompt-level behavior change in the processor's pipeline tool.** `conversation_id`/`contact_id` extracted from the conversation context now take precedence over whatever the model passed, a mismatch is logged as `[CRM-237] ignoring …`, and both fields are documented "DO NOT SET". Any agent instruction that relied on the model overriding the context stops having that effect, with no error surfaced (CRM-235/237/238). In the same service, the calendar's per-day business-hours config is now read regardless of the top-level `business_hours['enabled']` flag — an installation using that flag as a global off switch **starts returning bookable slots**.
- ⚠️ **Run the credential migration rakes after deploying the CRM:** `ai_credentials:migrate_dry_run` then `:migrate`, and `integration_credentials:migrate_dry_run` then `:migrate`. Both abort without writing if any consumer would change its effective secret. Skipping them degrades gracefully — `Ai::MigrationState` keeps the legacy `OPENAI_API_SECRET` / openai-hook fallback alive — but the old integration settings form no longer accepts a key.
- ⚠️ **New scheduled work.** The CRM adds `whatsapp_credential_probe_scheduler_job` (hourly, queue `low`): the scheduler must reload `schedule.yml` and a Sidekiq worker must actually drain `low`, or token-based WhatsApp channels are never re-probed. auth's `docker-entrypoint.sh` additionally runs `rails rbac:reconcile_super_admin` on every boot after `db:migrate` — failure prints a banner to stderr but does not abort; diagnose with `rbac:check_super_admin_drift`. Note that `account_owner` is never auto-reconciled (it is operator-editable); the drift check reports its missing catalog keys informationally only.
- ⚠️ **Verify segment and audience sizes before the first mass send.** The evo-flow fixes move segment counts in both directions on the first recompute, and expect heavier ClickHouse queries whenever the deleted-contacts cache is cold and for 15s after each ingested `contact.deleted`. Re-check anything alerting on journey-run error strings too: the 404 text is now contextual and a 422's message carries the CRM's own reason.
- ⚠️ **Swarm reproducibility: this tag pins nothing in the swarm stack.** `docker-compose.swarm.yaml` pulls every Evolution image from Docker Hub `:latest` — gateway, auth, auth_sidekiq, crm, crm_sidekiq, core, processor, bot_runtime, frontend and, as of this release, `evocrm_evoflow` too. `evo-flow` had been the one service left on the moving `:develop` tag ever since the swarm stack gained it the day before the v1.0.0 cut, so a swarm deploy mixed a released family with one service tracking develop; this release moves it to `:latest` like the rest. The file is now internally consistent, but it is still not pinned to a VERSION: alignment there is a function of what the registry holds at deploy time, not of the submodule pointers frozen in this tag. Replace the `:latest` tags with `1.1.0` before cutting if the deploy is meant to be reproducible.

### Repository housekeeping

- **CI arrived where it did not exist.** Five services gained their first executing test workflow in this window: auth (`.github/workflows/test.yml`, with three spec files excluded by name as a pre-existing red baseline), the frontend (typecheck + a scoped Vitest list + an `ESLint (diff)` gate that lints only the files changed against `origin/$BASE_REF`, refuses to pass on an unresolvable base ref, and reports — never fails on — a shrunken baseline; 170+ spec files previously shipped and never ran), the processor (`ci.yml`, so no PR had run pytest — including the PR that fixed EVO-2181), core-service (`go-test.yml`, building with `-modfile=go.community.mod` to mirror the image), and bot-runtime (`ci.yml` with a `redis:7-alpine` service container; `test/e2e` had sat non-compiling from EVO-558 through EVO-2180 without a single red check). The CRM added three guards of its own — `community-parity.yml`, `cascade-deletion-guard.yml` with `check-cascade-deletion-coverage.sh` and `.github/fk-deletion-allowlist.txt`, so a new FK can no longer silently break contact deletion (EVO-2187) — and grew `spec-staleness-guard.yml` substantially. The frontend also records its pre-existing lint debt in `eslint-suppressions.json` and puts i18n key parity across the six catalogues under CI.
- **Docs.** auth ships `docs/rbac-admin-access.md` documenting the no-bypass model. The CRM's `EXTENSION_POINTS.md` went from five points to six and the contract moved **2.0.0 → 2.1.0** with the new `permission_resolver` seam, whose `DEFAULT_IMPL` delegates to `EvoAuthService#check_user_permission` byte-for-byte so community behavior is unchanged. evo-flow documents the real journey-trigger contract in `docs/journey-manual-trigger.md` and `src/modules/journeys/README.md`. And the umbrella README's role model was corrected: the "Design principles (Community Edition)" section claimed *"No super-admin"* and a closed `account_owner` + `agent` hierarchy, both false — it now carries a Roles table citing `db/seeds/rbac.rb` as the single authoritative source, and states plainly for a security review that `super_admin` has **no single-holder guarantee** (audit `user_roles` rather than assuming one installation owner) and that the seeded three are not the whole set, because `account_owner` holds `roles.create` and can mint arbitrary custom roles at runtime (CRM-180).
- **Public-repo hygiene.** `internal/review/` was deleted in full — `review.sh`, the 324-line six-service `:develop`/`:pr-N` compose and its README — on the grounds that review lives on the board, not in the repository; `docs/code-review-evo-1354.md` (212 lines of adversarial-review rounds for a change in a **private** enterprise repo, with links and unfixed-finding narrative) went with it. The two dangling references were untangled rather than left broken: the v1.0.0-rc6 entry lost its review-environment sentence and housekeeping bullet, and `nginx/README.md` — which described the gateway's default underscore upstreams as "matching the review environment, the only stack that runs this gateway against the default hostnames" — now says plainly that **no stack in this repository** runs the gateway against those defaults. 727 lines deleted against 71 added across the umbrella window; anyone driving PR-image review stacks with `./internal/review/review.sh` loses that entry point, though the `:pr-N` images the services publish still exist.
- **The umbrella itself is env/compose only.** Verified by diff over `bcfe0e5..de087c5`: no change to `.github/workflows/`, the migration-collision guard scripts, the issue/PR templates, `setup.sh`, the `Makefile`, `docs/SETUP-GUIDE.md`, `docs/TROUBLESHOOTING.md`, or the gateway image (`nginx/Dockerfile`, `nginx/default.conf.template` — only README prose moved). No CI, release-automation or gateway-behavior change ships here; `make setup` behaves as before, and a fresh checkout now yields the two new env keys with dev-safe defaults. Beyond that range, the release itself adds two umbrella commits — the family pointer bump and the `evo-flow` swarm-tag fix — and nothing else.
- **Family pointer bumps, with the range each commit count was measured over** (`git rev-list --count <from>..<to>`, from the pointer recorded at `v1.0.0` to the frozen head of 2026-09-02):
  - `evo-auth-service-community` `2d9884f..851073d` — **105**
  - `evo-ai-crm-community` `ad7523c..aa3d408` — **335** (carries the CRM-187/EVO-2250 credential chain, which landed at `1e01c5a` inside this range, that the umbrella's `EVO_AI_ENCRYPTION_KEY` wiring depends on)
  - `evo-ai-frontend-community` `c8bc64e..20ba0e6` — **347**
  - `evo-ai-processor-community` `5769ec6..a8e1bd4` — **48**
  - `evo-ai-core-service-community` `729d691..c6f7ba3` — **78**
  - `evo-flow-community` `ddc5753..1734849` — **40**
  - `evo-bot-runtime` `040a486..7ec7db4` — **12** (the CRM-236 timeout/notice work the `AI_CALL_TIMEOUT_SECONDS=90` bump depends on)
  Total **965**. Outside the family tag set, `evolution-api` also moves, `dd552c7..e273b90` — **12**, with behavior change; `evolution-go` (`706c9a4`) and `evo-nexus` (`fe15fd5`) do not move. Against older images the two new umbrella variables are inert.

### Changed

- **Docs — README role model corrected (CRM-180)** — the "Design principles" section claimed *"No super-admin"* and a closed `account_owner` + `agent` hierarchy with "no intermediate roles". Both were false: `evo-auth-service-community` seeds a third role, `super_admin` (the only holder of `installation_configs.manage`), and `account_owner` holds `roles.create`, so custom roles exist at runtime. Replaced by a **Roles** subsection that states the real model, that `super_admin` has no single-holder guarantee (audit `user_roles` instead of assuming one installation owner), and that `Role::ADMIN_ROLE_KEYS` is an admin-bypass allowlist rather than the role model.

## [v1.0.0] - 2026-07-15

**The first stable release of the CRM Community family.** v1.0.0 promotes `v1.0.0-rc7` to GA after a focused stabilization/hardening pass — no new features of size; the window is bug fixes, security hardening, configuration-resolution corrections, multi-tenant/RLS wiring, and a frontend UI-redesign wave. ~135 commits across the family (CRM 43, processor 37, frontend 36, auth 11 net-new, core 8; `evo-bot-runtime` and `evo-flow-community` are alignment-only re-tags with zero delta). **No new database migrations in any service.**

**Family versioning is now unified — the auth "one rc ahead" exception is dropped.** Since rc6 the auth service ran one release-candidate ahead of the family (the rc7 family shipped auth as `v1.0.0-rc8`). For the stable cut that exception is gone: **every family service, auth included, ships as a single `v1.0.0`.** The auth `develop` head that was previously tagged `v1.0.0-rc8` (`2d9884f`) becomes `v1.0.0`; its delta below is measured from `v1.0.0-rc8` (11 net-new commits, a valid non-scrubbed range). `evolution-api`, `evolution-go` and `evo-nexus` keep their own version schemes and are **not** part of the `v1.0.0` tag set — they are pinned by pointer, unchanged since rc7.

Six themes:

1. **Custom Tools & MCP fixed end-to-end (EVO-2125, EVO-2139).** The MCP custom-server **"Test connection"** button — permanently broken because the Go side did a raw `GET /health` that every spec-compliant MCP 404s — is reworked family-wide: core-service now delegates to the processor's new `POST /custom-mcp-servers/test-connection` (mirroring the working `discoverTools` pattern, propagating `X-Evo-Tenant-Id`), the processor performs a **real MCP handshake** instead of a health check, and the frontend guards `tools: null` and shows the fresh discovery count (`tools_count`) instead of the DB-stale `server.tools.length`. **Custom HTTP tools** now bind object payloads (core retyped the `values` field from `map[string]string` to `map[string]interface{}`, fixing the wizard's `__evo_modes_meta__` object 400ing), the processor registers a tool once and rebuilds attached-by-id tools at runtime keeping metadata off the wire, and the frontend adds an attach-to-agent hint plus i18n-key fixes.

2. **RBAC & session-ownership hardening (EVO-2124 / 2127 / 2103 / 2123).** Auth now implies AI-agent `execute` from `write` so agent editors can actually run their own agents, and adds a **revocable** coarse read/write/delete write-action bridge that no longer 403s delegated admins (EVO-2124/2127); the processor aligns per-agent session gates to the `ai_agents` resource — session delete is now gated on `ai_agents.delete`, **closing a metadata-owner hole** — enforces session ownership on chat/read/delete (blocking cross-user session access), and accepts the `api_access_token` field name evo-auth actually sends over X-API-Key/WebSocket (EVO-2124/2103/2123); the frontend role editor persists the coarse write group on save (EVO-2127).

3. **Config resolution forced ENV-first + unauthenticated OAuth callbacks removed (EVO-2095 / 2096 / 2087 / 2089).** The CRM now resolves `config.active_storage.service` from `ENV.fetch('ACTIVE_STORAGE_SERVICE')` (production) and `config.action_mailbox.ingress` from `ENV.fetch('RAILS_INBOUND_EMAIL_SERVICE')` at boot instead of DB/`GlobalConfigService`, each single-source-of-truth with a dedicated CI staleness guard. Security: all unauthenticated OAuth **GET callback** routes/controllers were removed and the deprecated **Twitter connect flow** dropped; the dead `api/v1` Evolution by-events webhook route was removed while the per-event path is now accepted when Evolution posts with `WEBHOOK_BY_EVENTS=true`.

4. **Multi-tenant / RLS binding wired through processor & core (PR #24, EVO-1623).** The processor binds the tenant GUC (`app.current_tenant_id`) via a new `runtime_context.bind_context` extension point (async no-op in the community build, real `set_config` in the enterprise overlay) and forwards `X-Evo-Tenant-Id` when fetching OAuth credentials from the CRM; the core-service's enterprise tenantstamp extension point now reroutes the tenant-scoped connection on **Update and Delete**, not only Create — previously Update/Delete ran on a pooled connection without the GUC, so RLS failed closed and writes to `evo_core_*` silently affected 0 rows. Both are strict no-ops with zero behavior change in the community build.

5. **DB/connection resilience under Swarm (EVO-2141, asyncpg/alembic SSL).** The processor's DB pool is hardened against Docker Swarm idle-connection reaping (async engine `pool_pre_ping=True` + `pool_recycle=1800`, libpq TCP keepalives on the sync engine, `max_inactive_connection_lifetime=300` on the raw asyncpg pool), fixing intermittent 500s / "AI not responding". asyncpg now receives the raw libpq DSN (fixing `sslmode` handling for both `require` and `disable`), and the alembic online-migration engine rewrites `sslmode`→`ssl` for **any** value — so Swarm-overlay deployments running `sslmode=disable` stop silently skipping online migrations.

6. **Frontend UI-redesign wave + integrations OAuth overhaul.** Pipelines were rebuilt end-to-end (list/board/card-detail), the Agents (Atendentes) screen converted to list-only, and Channels (Canais) redesigned — overview collapsed into a single channel-type list, connect/new-channel reworked as a modal host, and the Channel Settings screen redesigned (EVO-2092/2093/2094). Integrations gained a full OAuth redirect/callback-envelope overhaul (redirect to the real OAuth URL, correct callback path, return to the agent Integrations tab, unwrap the processor envelope) and Google Calendar/Sheets were made available as agent integrations; the CRM adds a read-only `GET /api/v1/integrations/availability` endpoint (per-provider "configured?" booleans, never the secret) that drives ATIVAR vs "Em breve".

### Submodules updated

- **evo-auth-service-community** v1.0.0 — ⚠️ **The "one rc ahead" version exception is dropped this release**: since rc6 the auth service ran one rc ahead of the family (the rc7 family shipped auth as `v1.0.0-rc8`); for the stable cut auth ships as **`v1.0.0`** like every other service, cut at the `develop` head `2d9884f` that was previously tagged `v1.0.0-rc8`. The real delta (11 net-new commits, valid non-scrubbed range) is measured from `v1.0.0-rc8`. **(headline)** RBAC role-editor fixes: a role granted `write` on an AI agent now **implicitly gains `execute`** so agent editors are no longer blocked from running their own agents (EVO-2124), and the role editor's coarse read/write/delete bridge is made **revocable** and stops 403ing delegated admins (EVO-2127). CI adds a migration-version-parity gate (auth = ODD version numbers) with an RSpec check and CONTRIBUTING enforcement docs (EVO-2090) — tooling only. No new migrations, no new/renamed env vars; standard image redeploy. ⚠️ Behavioral: effective permissions widen for existing agent-editor roles (write⇒execute).
- **evo-ai-crm-community** v1.0.0 — Stabilization/hardening release (43 commits, no new features of size). **(headline)** Configuration resolution forced **ENV-first**: `ACTIVE_STORAGE_SERVICE` (production) and `RAILS_INBOUND_EMAIL_SERVICE` (action_mailbox.ingress, at boot) now resolve from the environment instead of DB/`GlobalConfigService`, each single-source-of-truth with a CI staleness guard (EVO-2095/2096); the admin config screen reads via `GlobalConfigService` so BYO/per-tenant config is still honored. Security: all unauthenticated OAuth GET callback routes/controllers removed and the deprecated Twitter connect flow dropped (EVO-2087); the dead `api/v1` Evolution by-events route removed while the per-event webhook path is accepted for `WEBHOOK_BY_EVENTS=true` (EVO-2089). New read-only `GET /api/v1/integrations/availability` (EVO/PR #220). Bug fixes: STI disabled on `Role` (no more `SubclassNotFound`, EVO-2128), pipelines backend (stage_type enum round-trip, pipeline-level `services_info` so Valor Total is no longer R$ 0,00), public forms resolvable by id or slug, evo-flow client answers 503 on missing config + real port, WhatsApp `provider_connection` reset on reconnect (EVO-2140). CI adds an EVEN migration-version-parity gate (EVO-2090). **No new migrations.** ⚠️ Deploy: `ACTIVE_STORAGE_SERVICE` and `RAILS_INBOUND_EMAIL_SERVICE` must now be provided as real env vars (DB/admin values for these are informational); `TWITTER_CONSUMER_KEY`/`TWITTER_CONSUMER_SECRET`/`TWITTER_ENVIRONMENT` are no longer read.
- **evo-ai-frontend-community** v1.0.0 — UI-redesign wave plus OAuth-callback fixes and small RBAC/custom-tools/MCP guards (~36 net commits; range has a valid merge-base and is not scrub-inflated). **(headline)** Pipelines redesigned end-to-end (list/board/card-detail, PR #245/#246); the Agents (Atendentes) screen converted to list-only (PR #247); Channels (Canais) redesigned — single channel-type list, connect/new-channel as a modal host, Channel Settings redesign (EVO-2092/2093/2094, PR #254–257). Agent config reorganized with an MCP wizard and fixed integrations client; a full integrations OAuth redirect/callback-envelope overhaul (redirect to the real OAuth URL, correct callback path, return to the agent Integrations tab, unwrap the processor envelope so calendars/spreadsheets read as a data array, PR #248–253); Google Calendar & Google Sheets made available as agent integrations (PR #250). Custom HTTP tools attach-to-agent hint + i18n fixes (EVO-2125), the RBAC role editor persists the coarse write group (EVO-2127), and Custom MCP guards null tools and uses `tools_count` in the success toast (EVO-2139). No migrations, no new env vars, no Docker/entrypoint changes. ⚠️ Admin-visible behavior: in the redesigned Channels screen the Email connect option is greyed out with a tooltip unless a Gmail/Outlook OAuth integration is configured (EVO-2092).
- **evo-ai-processor-community** v1.0.0 — Hardening release (37 commits; `develop` head `5769ec6`). **(headline)** DB connection pool made resilient to Docker Swarm idle-connection reaping (`pool_pre_ping` + `pool_recycle=1800` + libpq keepalives + asyncpg `max_inactive_connection_lifetime=300`), fixing intermittent 500s / "AI not responding" (EVO-2141); asyncpg gets the raw libpq DSN and the alembic online-migration engine rewrites `sslmode`→`ssl` for **any** value, so `sslmode=disable` Swarm overlays stop silently skipping online migrations (PR #38/#39). RBAC/security: per-agent session gates aligned to `ai_agents` with session delete gated on `ai_agents.delete` (closes a metadata-owner hole, EVO-2124); session ownership enforced on chat/read/delete and scoped to the logged-in user (EVO-2103); auth accepts the `api_access_token` field evo-auth actually sends and probes both token types on the WS handshake (EVO-2123). MCP test-connection does a real MCP handshake (EVO-2139); custom HTTP tools rebuilt-by-id at runtime with metadata off the wire (EVO-2125); multi-tenant tenant-GUC binding wired via `runtime_context.bind_context` (community no-op, enterprise overlay implements `set_config`) and `global_config_service` now reads `EVO_AI_CRM_URL` (PR #24). **No schema migration** (only `migrations/env.py` changed). ⚠️ New env `EVO_AI_CRM_URL` (set to the CRM address). ⚠️ Behavioral: roles lacking `ai_agents` permissions now 403 on session ops previously reachable via the metadata-owner hole; cross-user session access now fails.
- **evo-ai-core-service-community** v1.0.0 — Bug-fix release (8 commits). **(headline)** MCP "Test connection" reworked to delegate to the processor's `POST /custom-mcp-servers/test-connection` instead of the broken raw `GET /health`, propagating `X-Evo-Tenant-Id` and normalizing `Tools nil`→`[]` so `"tools": null` never reaches HTTP clients; `tools_count` is always emitted (EVO-2139). Custom Tools now accept object payloads (`values` retyped `map[string]string`→`map[string]interface{}`, column already `type:json`, no migration, EVO-2125). The enterprise tenantstamp extension point now reroutes the tenant-scoped connection on Update/Delete, not only Create — fixing RLS-failed-closed silent 0-row writes under the enterprise build (EVO-1623, no-op in community). httpclient error mapping now surfaces DB-layer codes/status (duplicate key → 409, record not found → 404) instead of falling through to 500. No new migrations or env vars. ⚠️ Cross-service coupling: MCP test-connection requires the deployed processor to expose `POST /custom-mcp-servers/test-connection` (satisfied by co-deploying the family at v1.0.0).
- **evo-flow-community** v1.0.0 — Version-alignment re-tag; identical tree to `v1.0.0-rc7` (`develop` at `ddc5753`). No commits since rc7 — the event-tracking service (evo-flow + ClickHouse) is re-tagged solely to keep the family image set on a single stable version. No features, migrations, env vars or Dockerfile changes.
- **evo-bot-runtime** v1.0.0 — Version-alignment re-tag; identical tree to `v1.0.0-rc7` (`develop` at `040a4863`), so the Go binary is byte-identical to rc7. The tag simply realigns the bot-runtime image with the rest of the family. Last functional content is unchanged from rc6 (restored agent conversation memory over a2a, media-aware dispatch, per-PR review images).

### Non-family submodule pointers

- **evolution-api** (`dd552c7`), **evo-nexus** (`fe15fd5`) and **evolution-go** (`706c9a4`) — all **unchanged since rc7**. Not part of the `v1.0.0` family tag set; they keep their own version schemes and are recorded here for traceability only.

### Notes for upgrading an existing PROD

- ✅ **No new database migrations in any service.** Standard image redeploy; the delta introduces no `db:migrate`/alembic step beyond the usual.
- ⚠️ **Version tags — pull `:1.0.0` for the whole family, auth included.** The auth "one rc ahead" exception is dropped: auth ships as `v1.0.0` (was `v1.0.0-rc8` for the rc7 family). Co-deploy the family at a single `v1.0.0` — this also satisfies the EVO-2139 cross-service coupling (core-service's MCP test-connection needs the matching processor endpoint).
- ⚠️ **CRM — `ACTIVE_STORAGE_SERVICE` now ENV-first (EVO-2095).** In production the storage service resolves from the `ACTIVE_STORAGE_SERVICE` env var only; the admin-config/DB value no longer overrides it at boot and is informational. Deployers who set the storage provider via the admin DB **must set the env var explicitly** or storage silently changes.
- ⚠️ **CRM — `RAILS_INBOUND_EMAIL_SERVICE` now ENV-first (EVO-2096).** `config.action_mailbox.ingress` resolves from the env var at boot instead of DB/`GlobalConfigService`; the DB/admin value is informational only. Set the env var to control inbound-email ingress.
- ⚠️ **CRM — Twitter connect flow removed (EVO-2087).** The deprecated flow and its unauthenticated OAuth GET callback routes/controllers are gone; any integration on the old Twitter OAuth1.0a connect/callback URLs will break. `TWITTER_CONSUMER_KEY`/`TWITTER_CONSUMER_SECRET`/`TWITTER_ENVIRONMENT` are no longer read — safe to drop.
- ⚠️ **CRM — dead Evolution by-events webhook route removed (EVO-2089).** Ensure Evolution webhooks target the supported per-event path (`WEBHOOK_BY_EVENTS=true` is now accepted on the correct path).
- ⚠️ **Processor/auth — RBAC & session-ownership tightening is breaking.** Session delete is now gated on `ai_agents.delete` and per-agent session gates map to `ai_agents`, so roles lacking `ai_agents` permissions get 403 on session ops they previously reached via the metadata-owner hole (EVO-2124); sessions are enforced per-owner on chat/read/delete, so cross-user session access now fails (EVO-2103). In auth, `write` on an AI agent now implies `execute`, widening effective permissions for existing agent-editor roles (EVO-2124). Verify roles grant the expected `ai_agents` keys before rollout.
- **Processor — new env `EVO_AI_CRM_URL`** (PR #24): now read as the preferred CRM base URL (falls back to `EVOLUTION_BASE_URL`, then `http://localhost:3000`). Set it to the CRM address.
- **Processor — Swarm resilience (no config change).** The DB pool is hardened against idle-connection reaping (EVO-2141) and `sslmode=disable` Swarm overlays now run alembic online migrations correctly instead of silently skipping them (PR #38/#39).
- **Frontend — no migrations, no new env vars, no Docker changes.** Admin-visible only: Email connect is greyed out unless Gmail/Outlook OAuth is configured (EVO-2092).
- **Enterprise overlay only:** the processor tenant-GUC binding (`runtime_context.bind_context`) and the core-service tenantstamp Update/Delete reroute (EVO-1623) are strict no-ops in community; the real `set_config`/RLS binding is provided by the enterprise overlay.

### Repository housekeeping

- **Family versioning unified.** The auth "one rc ahead" exception is retired for the stable cut; all family services carry a single `v1.0.0` tag. `evolution-api`/`evolution-go`/`evo-nexus` remain out of the family tag set, pinned by pointer, unchanged since rc7.
- **CI migration-version-parity gates (EVO-2090).** Dedicated parity guards landed in both the CRM (enforces EVEN migration version numbers) and auth (enforces ODD), each with an RSpec check and enforcement docs. Tooling/guard only — no runtime change.
- **Family pointer bump to the stable heads:** auth `2d9884f`, crm `ad7523c`, frontend `c8bc64ea`, processor `5769ec6`, core `729d691`; flow (`ddc5753`) and bot-runtime (`040a4863`) unchanged (re-tag only).

## [v1.0.0-rc7] - 2026-07-09

An authorization-hardening window: rc7 closes the cross-service **RBAC enforcement audit** that rc6 opened (the EVO-2035 epic) and layers on deploy-time **boot hardening** for orchestrators that ignore the compose command. A much smaller window than rc6 (~5 days, ~90 net commits across the family — CRM 32, frontend 31, auth 21 net-new, processor 6; core, flow and bot-runtime are alignment-only re-tags with zero delta). Three main themes:

1. **RBAC enforcement audit closes family-wide.** In the CRM, every mutating `/api/v1` route is now permission-gated and kept honest by a guard spec that walks each routed POST/PUT/PATCH/DELETE and fails on any ungated action — contact merge/bulk/notes/labels (EVO-2024), WhatsApp channel management (EVO-2023), upload/Facebook callbacks/search (EVO-2025), global OpenAI event processing now requiring `integrations.execute` (EVO-2031), conversation transcript and pipeline-item writes (EVO-2034), and OAuth-secret regeneration plus Pundit-gated scheduled-action/pipeline-task creation (EVO-2061). The processor confines agent-bot **runtime keys** to an explicit allowlist so a leaked key can no longer delete the agent or read/write OAuth credentials, and gates the chat WebSocket handshake (EVO-2027). The auth service trims 15 dead/duplicated resources from the catalog down to **48 resources / 270 keys**, adds a `system:true` action flag so the permissions screen can hide runtime keys, consolidates `team_members.*`→`teams.*` and `permissions.read`→`roles.read`, and drops the dead `agents.*` twin in favor of `ai_agents` (EVO-2070/2072/2034/2060). The frontend rebuilds the role permissions editor as a fixed-column table grouped by domain with search/descriptions and rewires all enforcement onto a single `PermissionsContext` (EVO-2069/2071/2060/2033/2030/2028/2031/2029).

2. **Inbox visibility becomes strictly permission/membership-driven (EVO-2057).** A user with zero `inbox_member` rows no longer sees every inbox; `assigned_inboxes` resolves only from the admin role or the `conversations.read_all` grant the auth upgrade migration handed to pre-existing roles. This is what makes agent restriction actually enforceable — but it means the matching auth migration **must** have run to preserve today's visibility.

3. **Deploy / boot hardening.** The auth and CRM images now migrate on boot behind a `RUN_MIGRATIONS` gate (default `true`, fail-safe) via a dedicated entrypoint, so orchestrators that ignore the compose command (CapRover) still migrate instead of serving a stale schema (EVO-1999); CRLF→LF normalization on shell/entrypoint scripts stops `exec …\r: no such file or directory` exit-127 / 502 boots (EVO-2011 auth+CRM, EVO-2020 frontend). The CRM backend goes **API-only by default** — `CW_API_ONLY_SERVER` renamed to `EVOLUTION_API_ONLY_SERVER`, default flipped to `true`, SPA served by evo-frontend — and channel OAuth callbacks now redirect to the frontend via a new `FrontendRedirectable` concern instead of 500ing on unregistered `app_*` helpers (EVO-2010, EVO-2014).

As in rc6, the **auth service ships one rc ahead of the family**: the rc6 family shipped auth as `v1.0.0-rc7`, so the rc7 family ships auth as **`v1.0.0-rc8`**, cut at the scrubbed `develop` head (`70ea4af`). The standalone `v1.0.0-rc7` auth tag (`16ee125` → `bf99a8e`, cut 2026-07-04) now sits on the pre-scrub history line superseded by the enterprise-leak-removal force-push and **must not be deployed** — it predates the scrub and still carries the internal-only enterprise/whitelabel identifiers the rewrite removed. On the umbrella itself: a migration-execution guard (`RUN_MIGRATIONS=false` on services that must not migrate, EVO-1999 / PR #154), a DB-side recovery runbook for the skipped-migration version collision (EVO-1911), and the family pointer bump for auth/crm/frontend/processor (EVO-2069 / PR #157).

### Submodules updated

- **evo-auth-service-community** v1.0.0-rc8 — ⚠️ **Version-number exception (continues the rc6 pattern)**: the auth service runs one rc ahead of the family. The rc6 family already shipped auth as `v1.0.0-rc7`; the family rc7 therefore ships auth as **`v1.0.0-rc8`**, cut at the scrubbed `develop` head `70ea4af`. The standalone `v1.0.0-rc7` auth tag (`16ee125` → `bf99a8e`, cut 2026-07-04) is on the pre-scrub history line superseded by the enterprise-leak-removal force-push and is **superseded — do not deploy it** (it predates the scrub; `rc7...rc8` compare is disjoint and non-meaningful, the real delta is 21 net-new commits). **(headline)** RBAC catalog hygiene (EVO-2070/2072/2034/2060): trims 15 dead/duplicated resources down to 48 resources / 270 keys, adds a `system:true` action flag so the permissions screen can hide runtime keys, consolidates `team_members.*`→`teams.*` and `permissions.read`→`roles.read`, and drops the dead `agents.*` twin in favor of `ai_agents` — `ai_folders`, `ai_mcp_servers` and `ai_tools` are deliberately kept (still enforced live by core-service/processor). Account-settings hardening (EVO-2026): the privileged-key guard is extended over the `settings`/`custom_attributes` blob, account-update and users-role endpoints are gated, and authorization now fails closed on unmapped actions. Email-confirmation posture (EVO-2016) now derives from **SMTP presence** via `GlobalConfigService` instead of the `REQUIRE_EMAIL_CONFIRMATION` switch — SMTP present ⇒ new signups must confirm, absent ⇒ open — with a data migration amnestying pre-existing unconfirmed accounts. New `integrations.execute` permission granted to admin roles (EVO-2031). Deploy: the image now migrates on boot via a dedicated `docker-entrypoint.sh` (`RUN_MIGRATIONS` gate, default `true`; set `false` on the sidekiq role) with a build-arg `RAILS_ENV` (`development` default, `--build-arg RAILS_ENV=production` for deploy images), fixing CapRover deployments that ignore the compose command (EVO-1999), plus CRLF→LF script normalization to stop 502/exit-127 boots (EVO-2011). Ships 6 new migrations (`20260622120001` renumber + `20260706191528`..`20260709120000`). ⚠️ The auth repo's own `CHANGELOG.md` still tops out at `[v1.0.0-rc7]` (pre-scrub) and does not document this 21-commit delta.
- **evo-ai-crm-community** v1.0.0-rc7 — RBAC enforcement-hardening release that closes the CRM half of the authorization audit (32 commits). **(headline)** Every mutating `/api/v1` route is now permission-gated and kept honest by a guard spec that walks each routed POST/PUT/PATCH/DELETE and fails on any ungated action (EVO-2034): contact merge/bulk/notes/labels (EVO-2024), WhatsApp channel management (EVO-2023), upload/Facebook callbacks/search (EVO-2025), global OpenAI event processing now requiring `integrations.execute` (EVO-2031), write actions left out of gated controllers (EVO-2032), conversation transcript and pipeline-item writes (EVO-2034), and OAuth-secret regeneration plus Pundit-gated scheduled-action/pipeline-task creation (EVO-2061). Inbox visibility becomes strictly permission/membership-driven — a user with no `inbox_member` rows no longer sees every inbox; `assigned_inboxes` resolves only from the admin role or the `conversations.read_all` grant the auth upgrade migration handed to pre-existing roles, and the OAuth path resolves it identically (EVO-2057). The in-repo permission-catalog mirror was resynced with the auth SSOT (EVO-2070, 54 dead keys trimmed), the `AgentsController` gate repointed to `ai_agents.*` (EVO-2072), and four route-less `ai_*` controllers plus the entire reports-v2 builder/view/helper surface — both superseded by the Go core-service — were deleted. Deploy/ops: the image now boots via `rails.sh` (ENTRYPOINT) behind a `RUN_MIGRATIONS` gate (default `true`) so orchestrators that ignore the compose command like CapRover still migrate on boot, with the 2025/2026 migrations made idempotent (EVO-1999); Docker CRLF/LF fix for `bin/*` and ENV `key=value` format (EVO-2011); and a `facebook_webhook_logger` boot guard (EVO-2012). The backend goes API-only by default — `CW_API_ONLY_SERVER` renamed to `EVOLUTION_API_ONLY_SERVER`, default flipped to `true`, SPA served by evo-frontend (EVO-2010) — channel OAuth callbacks now redirect to the frontend via the new `FrontendRedirectable` concern, and the never-functional CRM onboarding flow was removed (EVO-2014, EVO-2013). Ships one self-healing migration that re-adds `messages.source` on databases bitten by the CRM/auth version collision (EVO-1911).
- **evo-ai-frontend-community** v1.0.0-rc7 — RBAC/permissions-surface hardening release (31 net commits over rc6; the raw `rc6..develop` range shows ~440 because the history was rewritten by the leak-removal scrub and the two share no merge-base). **(headline)** the role permissions editor is rebuilt as a fixed-column table with per-section and top-level bulk select, collapses every resource to Read/Write/Delete, groups permissions by domain with search/filter and descriptions, hides permissions that should not exist (`ai_tools` and other phantom keys) while no longer stripping system grants, and locks basic/implied permissions so they toggle only when the source grant is held (EVO-2069, EVO-2071, EVO-2060). Enforcement is rewired onto a single `PermissionsContext` (dropping `useUserPermissions`, EVO-2033): write controls render only when the matching grant is present (EVO-2030), the channels namespace is unified to `inboxes.*` with ChannelSettings sender/signature/avatar controls gated (EVO-2028), cross-backend capabilities and the Account menu are gated (EVO-2031), dashboard-apps gates are repointed to their own resource with the bots placeholder dropped (EVO-2029), the MCP gate is reverted to `ai_mcp_servers` and the dead reports UI/service removed (EVO-2070), and the static permissions catalog mirror is synced against the live catalog and marked non-authoritative with render-gate coverage (EVO-2072, EVO-2034). Also lands setup-wizard extension slots (a `setup.steps` plugin slot + host context delivering slot controls by prop for cross-bundle safety), a contacts UI overhaul (inline edit on dedicated pages replacing modals, contact notes, a filter popover, a social-profiles field, 6-locale i18n), chat-sidebar advanced-filter/status-leak fixes, and per-PR `:pr-N` review images plus an internal-identifier CI guard (EVO-1998). No migrations and no new env vars; the only infra change is EVO-2020 — `.gitattributes` forces `*.sh eol=lf` and the Dockerfile strips CR from the entrypoint, fixing Windows-checkout image builds that crash-looped with `exec /docker-entrypoint.sh: no such file or directory`.
- **evo-ai-processor-community** v1.0.0-rc7 — Security-hardening release focused on agent-bot / runtime-key authorization (EVO-2027, part of the EVO-2035 RBAC audit epic). `verify_agent_access`, previously stubbed to always allow, is implemented: it fails closed on missing user context, confines agent-bot credentials to their own agent, and keeps the single-tenant pool access for regular users. The agent-bot bypass in `validate_permission` — which had granted any permission on the bot's own agent path, so a leaked runtime key could delete the agent or read/write OAuth credentials — is now confined to an explicit runtime allowlist (agent `execute`; a2a `execute`/`read`/`task_management`; chat-session `read`) and fails closed on everything else, including all integration credential actions. The substring path-scope check is replaced with canonical path-segment extraction so an agent id embedded mid-token can no longer spoof scope, and the folder-share lookup now fails closed instead of degrading to a silent pool grant. The chat WebSocket routes `/ws` and `/ws-live`, which authenticate inline and never reach the HTTP `RequirePermission` gate, now reject an agent-bot key at the handshake when it targets a different agent or requests a non-runtime action, via the shared `PermissionService.is_agent_bot_permission_allowed` helper; regular-user access is unchanged (+331 lines of authz unit coverage). CI closes out EVO-1998: the publish workflow now builds and pushes the `:develop` image on push to develop (with `:develop-<sha>` for traceability) — staging had silently run stale code — adds `workflow_dispatch` and fails fast when a run resolves no publish tags. No new migrations, environment variables or Dockerfile changes.
- **evo-ai-core-service-community** v1.0.0-rc7 — No functional change since rc6; version-alignment bump to keep the family on a single release-candidate tag. `origin/develop` is unchanged at `bdec2498`, the exact commit `v1.0.0-rc6` points to, so rc7 re-tags the same tree the rc6 family already shipped: server-side advanced filtering on the Agents (EVO-1952) and Custom Tools + Custom MCP Servers (EVO-1953) list/count endpoints, the content-type-agnostic Custom Tool Test endpoint with SSRF guard (EVO-1790), the standalone `go.community.mod` community image plus per-PR review images (EVO-1998), and the enterprise multi-tenancy extension points (EVO-1621/1623/1624/1625) and license-guardian boot hook (EVO-1989) which remain strict no-ops behind build tags. No new migrations or environment variables for community deployments.
- **evo-flow-community** v1.0.0-rc7 — Version-alignment bump only: no commits landed on develop since its debut `v1.0.0-rc6` (2026-07-04), so `origin/develop` still sits at `ddc5753` and the Docker image content is identical to rc6. The event-tracking service (evo-flow + ClickHouse, added under EVO-1571) is re-tagged rc7 solely to keep the family image set on a single rc number; no new features, migrations, environment variables or Dockerfile changes.
- **evo-bot-runtime** v1.0.0-rc7 — Version-alignment bump only, no functional change. `origin/develop` sits at the same commit (`040a4863`) as the `v1.0.0-rc6` tag, so the range is empty and the Go binary is byte-identical to rc6; the rc7 tag simply realigns the bot-runtime image with the rest of the family, mirroring the earlier rc5/rc3 catch-up realigns. Last functional content remains the rc6 headline — restored agent conversation memory over a2a, media-aware dispatch, and per-PR review images.

### Non-family submodule pointers

- **evolution-api** (`dd552c7`), **evo-nexus** (`fe15fd5`) and **evolution-go** (`706c9a4`) — all **unchanged since rc6**. Not part of the family tag set; recorded here for traceability.

### Notes for upgrading an existing PROD

- ⚠️ **Deploy order — auth first/alongside the CRM.** The CRM rc7 RBAC enforcement assumes the matching auth rc7 is deployed: EVO-2070 trimmed the auth `ResourceActionsConfig` (and the CRM catalog mirror was resynced to it), and EVO-2057 depends on the auth upgrade migration that granted `conversations.read_all` to pre-existing roles. A version skew (old auth + new CRM) yields 403s on newly-gated endpoints and/or agents losing all inbox visibility.
- ⚠️ **CRM/frontend/processor — RBAC enforcement is breaking.** Many previously-open `/api/v1` mutating routes (contact merge/bulk/notes/labels, WhatsApp channel mgmt, uploads, Facebook/OAuth callbacks, search, OpenAI event processing, pipeline items/tasks, scheduled actions, oauth-secret regen) now require explicit permissions (EVO-2023/2024/2025/2031/2032/2034/2061), and agent-bot runtime keys in the processor lose their prior broad bypass (EVO-2027). Any external OAuth client, integration, or under-privileged role relying on old open access will start receiving 403 — verify roles grant the expected keys before rollout, and validate the runtime/a2a and chat-bot WebSocket flows after upgrade.
- **auth — 6 new migrations** (`20260622120001` + `20260706191528`..`20260709120000`): the `grant_rbac_split` renumber (EVO-1911, the old `20260622120000` file is gone), `grandfather_unconfirmed_users_for_email_posture` (EVO-2016, no retroactive lockout), `revoke_accounts_update_from_agent_role` (EVO-2026), `grant_integrations_execute_to_admin_roles` (EVO-2031), `cleanup_rbac_catalog_grants` (EVO-2070 — reassigns `team_members`→`teams` / `permissions.read`→`roles.read`, idempotent), and `drop_dead_agents_permission_grants` (EVO-2072). Run `db:migrate` on upgrade.
- **CRM — 1 new migration** `20260705120000_heal_messages_source_after_version_collision` (EVO-1911): re-adds `messages.source` only if absent; no-op on healthy DBs. Repairs installs where the CRM/auth version collision at `20260622120000` left the column missing (Message model crashed at boot with "Undeclared attribute type for enum source").
- ⚠️ **Behavior change — email-confirmation posture (auth, EVO-2016).** `REQUIRE_EMAIL_CONFIRMATION` is demoted from switch to explicit override; the default posture now derives from SMTP presence resolved via `GlobalConfigService` (`installation_configs` → `runtime_configs` → ENV `SMTP_ADDRESS`). Installs that ran open **with SMTP configured** will start **requiring email confirmation for new signups** after upgrade (existing accounts are grandfathered — no lockout, but the signup UX changes). Operators relying on the old flag semantics should set it explicitly.
- **auth — new env `RUN_MIGRATIONS`** (default `true`) + custom `ENTRYPOINT` + build-arg `RAILS_ENV` (EVO-1999): the image runs `db:create`/`db:migrate` on boot (fail-safe; aborts boot on retry exhaustion) even on orchestrators that ignore the compose command. **Set `RUN_MIGRATIONS=false` on the sidekiq role** so it does not migrate in duplicate (a Postgres advisory lock protects concurrent replicas). Deploy images **must** be built with `--build-arg RAILS_ENV=production` (docker-publish.yml already bakes this in).
- **CRM — new env `RUN_MIGRATIONS`** (default `true`) + Dockerfile ENTRYPOINT `rails.sh` (EVO-1999): the web migrates on boot even on CapRover-style orchestrators. **Sidekiq/worker services must set `RUN_MIGRATIONS=false`** to avoid preparing the database twice.
- ⚠️ **CRM — env rename + default flip (EVO-2010).** `CW_API_ONLY_SERVER` is now `EVOLUTION_API_ONLY_SERVER` and defaults to `true` (backend is API-only, SPA served by evo-frontend). Set `EVOLUTION_API_ONLY_SERVER=false` only for a legacy setup where the backend itself serves dashboard routes. Update any env/compose still referencing the old key.
- **CRM — removed key `EVOLUTION_INSTALLATION_ONBOARDING`** and the `/installation/onboarding` endpoints (EVO-2014): the CRM onboarding flow never worked in community (first admin is created by the auth `/setup` wizard). Safe to drop from any env/config that still sets the flag.
- **frontend — no migrations, no new env vars.** Only infra change is EVO-2020 (build-time): `.gitattributes` forces `*.sh eol=lf` and the Dockerfile strips CR from `docker-entrypoint.sh`, fixing Windows-checkout image builds that crash-looped with `exec /docker-entrypoint.sh: no such file or directory`. Linux/CI builds were never affected. RBAC key note: the editor reverts the MCP gate to `ai_mcp_servers` and removes the `reports` resource — deploy alongside an auth/CRM/core catalog using the same keys or affected grants fail to resolve.
- **processor — no migrations, no new env, no Dockerfile change**; the upgrade is the image tag. Behavioral only: agent-bot runtime keys are confined to the runtime allowlist (EVO-2027). Deploy note (EVO-1998): merges to develop now rebuild the `:develop` image, so staging tracks current code again.
- **core-service / evo-flow / bot-runtime — none.** No migrations, no new env vars, no compose/Dockerfile changes since rc6. For these three, rc7 is a no-op image-tag change (same underlying commit as rc6). bot-runtime carry-over (unchanged from rc6): deploy it after the CRM that consumes the `Attachments` postback field; version skew degrades gracefully.

### Repository housekeeping

- Umbrella migration-execution guard (EVO-1999, PR #154): compose sets `RUN_MIGRATIONS=false` on services that must not migrate (only the owning service migrates); review follow-up translated comments to English and removed the redundant sidekiq entrypoint.
- Skipped-migration recovery runbook (EVO-1911): a DB-side recovery runbook (the `rails runner` path crashes in the already-broken environment) plus documentation of both real-world symptoms of the skipped migration.
- Family pointer bump (EVO-2069, PR #157 / merge `056f8fa`): auth `70ea4af`, crm `34628287`, frontend `15582db`, processor `9bb90d0`; core, bot-runtime and flow unchanged.
- ⚠️ **History rewrite (auth + frontend):** both services' `develop` was force-pushed by the enterprise-leak-removal scrub, so their `v1.0.0-rc7`/`v1.0.0-rc6` compare links walk disjoint histories and inflate the commit count (auth "175", frontend "~440"). Treat those compare links as non-meaningful for this release; the true deltas are 21 (auth net-new) and 31 (frontend). The pre-scrub rc6 tags of these two are superseded and should not be deployed post-rewrite.

## [v1.0.0-rc6] - 2026-07-04

The largest release-candidate window of the cycle (~5 weeks since rc5, ~300 commits across the family; the CRM alone ships 123 and the frontend 125). Three main themes:

1. **Media/storage overhaul** — ActiveStorage now defaults to `:local` with graceful bucket fallback (EVO-1961), and outbound media is served through the app proxy via the new `ATTACHMENT_DELIVERY=proxy` default so S3/MinIO/disk endpoints stay private (EVO-2006, EVO-1747, EVO-1940). The umbrella closes the loop with swarm/prod-test parity fixes for local storage, processor password and gateway media (EVO-1966).
2. **Global Message Templates cutover** — templates are decoupled from channels end-to-end: dedicated CRUD endpoint, WABA-wide Cloud approval sync, legacy-template data migration and a shared variable resolver in the CRM (EVO-1231/1232/1234/1235/1267/1716–1720), a unified global template screen with template-mode Send Message node in the frontend (EVO-1907, EVO-1233/1235/1255/1267, EVO-1760), and a new `message_templates` RBAC resource in the auth service (EVO-1233, EVO-1716).
3. **Security/RBAC hardening across the family** — the processor closes the EVO-1956 audit (82 integration handlers across 12 provider modules were reachable by any authenticated user, now gated with `RequirePermission`), the CRM gains per-inbox RBAC granularity and a permission-enforced Segments proxy (EVO-1938), server-side contact PII masking for non-admins lands across REST, ActionCable and background jobs (EVO-1551), and `EVOLUTION_HUB_API_KEY` is now encrypted at rest.

Also headline-worthy: **agent conversation memory restored** — the bot-runtime was keying ADK sessions on numeric display ids instead of UUIDs, so the agent answered every message from scratch ("404/500 Session not found"); it now sends the conversation UUID as `contextId` and the contact UUID as `userId`. New capabilities include a full **SendGrid email channel** (EVO-1248–1251, EVO-1721), **B14 CRM Lead Capture** (form-builder + chat pages + public pages, EVO-1771), **conversations history import** (EVO-1557), **products CSV bulk import with dry-run** (EVO-1555, EVO-1736, EVO-1783), a **Segments drag-drop canvas builder** routed through the CRM proxy (EVO-1247, EVO-1569), and six pipeline-oriented Journey nodes/triggers behind a pre-activation validation framework (EVO-1744, EVO-1742, EVO-1273/1272/1265/1257/1256/1266).

On the umbrella itself: swarm/prod-test parity fixes (EVO-1966 — local storage, processor password, gateway media, healthchecks for evo_crm/evo-core/evo-frontend, guaranteed bring-up from a clean checkout), compose/env fixes (`VITE_EVOFLOW_API_URL` for the frontend EVO-1906, `EVO_AUTH_BASE_URL` for the processor EVO-1683, Temporal healthcheck EVO-1755), and a **new submodule**: `evo-flow-community` with a local evo-flow + ClickHouse stack for event tracking (EVO-1571).

### Submodules updated

- **evo-auth-service-community** v1.0.0-rc7 — ⚠️ Version-number exception: the auth service already had a `v1.0.0-rc6` tag cut on 2026-06-12, but the family rc6 depends on fixes that landed after it, so the auth ships as **rc7** (cut at the head of develop, `5760306`); the standalone auth `v1.0.0-rc6` is superseded and should not be deployed with this family. **(headline)** EVO-1964: the refresh cookie is now scoped via `public_suffix` (e.g. `Domain=.refletia.com.br` instead of the rejected `.com.br`), fixing 401s on `/auth/refresh`/`/auth/validate` and WebSocket 440 on `.com.br` domains; `COOKIE_DOMAIN` becomes an optional override. Ships the 4 RBAC migrations the rc6 family expects: operational-read vs admin-gate permission split, `crm_forms` + `chat_pages` resources (B14 Lead Capture, EVO-1771), `conversations.import` (EVO-1557), and revoke of admin Settings from the agent role (EVO-1938). The setup wizard now captures whitelabel branding (title/color persisted, whitelabel flag exposed in `/setup/status`). Carried over from the superseded rc6 tag: `:latest` republish + fresh-install smoke harness (EVO-1404), new `message_templates` RBAC resource (EVO-1233, EVO-1716), admin-only `mask_contact_pii` toggle with deep-merge of partial `PATCH /api/v1/account` payloads (EVO-1551), email-confirmation flow plus GitHub OAuth with runtime credential resolution, dynamic BMS provider URL, automatic enterprise `evolution_admin` grant for the bootstrap user, and per-PR `:pr-N` review images (EVO-1998). Operators who pulled `:latest` between 2026-05-27 and 2026-06-12 must re-pull.
- **evo-ai-crm-community** v1.0.0-rc6 — By far the largest delta of the family (123 commits). **(headline)** Media/storage overhaul: ActiveStorage now defaults to `:local` with graceful bucket fallback (EVO-1961) and attachments are served through the app proxy via the new `ATTACHMENT_DELIVERY=proxy` default, keeping S3/MinIO/disk endpoints private (EVO-2006, EVO-1747, EVO-1940). Message templates complete their cutover from channel-coupled to global — dedicated CRUD endpoint, WABA-wide Cloud approval sync, legacy data migration and a shared variable resolver (EVO-1231/1232/1234/1235/1267/1716–1720). New capability: full SendGrid email channel (EVO-1248–1251, EVO-1721), B14 Lead Capture form-builder + chat pages (EVO-1771), conversations history import (EVO-1557), products bulk import with dry-run (EVO-1555, EVO-1736), and server-side PII masking for non-admin users hardened across REST, ActionCable and background jobs (EVO-1551). Deep repair series land on the automation engine (EVO-1635/1638/1640/1641/1642/1751), pipelines (stage inactivity actions, N+1 elimination, FK heal EVO-1845), label tagging persistence (EVO-1897/1928/1932/1863) and WhatsApp channel state (EVO-1967, EVO-1748, EVO-2007). Evolution Hub gains IG/FB DM ingestion fixes, channel auto-healing, template re-sync (EVO-1827) and at-rest encryption of the Hub API key; RBAC gets per-inbox granularity and the Segments proxy is permission-enforced (EVO-1938). Ships 15 new migrations.
- **evo-ai-frontend-community** v1.0.0-rc6 — By far the largest frontend release of the rc cycle (125 commits). Headline: **global Message Templates management** — a unified template screen reusing the channel components, flat dedicated endpoint, template mode with variable source mappings on the Send Message node, and template pickers for inbox greeting/out-of-office (EVO-1907, EVO-1716, EVO-1233/1235/1255/1267, EVO-1760). The **Journey/Flow builder** gains a pre-activation validation framework, a structured webhook body builder, and six pipeline-oriented nodes/triggers (Create Pipeline Task, Move to Pipeline Stage, Assign to Pipeline, Send Canned Response, pipeline-stage condition, Pipeline Stage Changed trigger — EVO-1744, EVO-1742, EVO-1273/1272/1265/1257/1256/1266), backed by a committed node-type manifest as cross-repo parity source (EVO-1634, EVO-1935) and a long tail of persistence/validation fixes (EVO-1850, EVO-1903/1904/1905, EVO-1889, EVO-1902, EVO-1945/1946). Chat gets a conversation-list overhaul with numeric unread badges and a channel hub showing real live connectivity (EVO-1960, EVO-1550, EVO-1674), plus a full-width composer, mobile polish, and a "Return to bot" action (EVO-1884, EVO-1782/1869, EVO-1680). CRM adds the Lead Capture UI (form + chat builders and public pages, EVO-1771), contact PII masking for non-admins (EVO-1551), products CSV bulk import (EVO-1734), a Segments drag-drop canvas builder (EVO-1247), end-to-end list filters for Agents/Custom Tools/MCP Servers (EVO-1952/1953, EVO-1937/1939), and RBAC-gated agent management (EVO-1938). Deploy-wise, the image now injects `VITE_EVOFLOW_API_URL` (EVO-1906) and the entrypoint extends the CSP to plain-http media/API origins for non-TLS self-hosted installs (EVO-1961).
- **evo-ai-processor-community** v1.0.0-rc6 — Security and correctness release. Closes the processor half of the EVO-1956 RBAC audit: the bulk integrations endpoint and all 12 third-party provider modules (GitHub, Google Calendar/Sheets, Notion, Linear, Monday, Atlassian, Asana, HubSpot, PayPal, Canva, Supabase) were reachable by any authenticated user — 82 handlers are now gated with `RequirePermission` by operation semantics, with a contract test locking the invariants (OAuth callback routers intentionally remain unauthenticated by design). Fixes OpenRouter key routing in LiteLLM (EVO-1684) — model ids are now normalized with the `openrouter/` prefix and routed to the OpenRouter api_base, eliminating AuthenticationErrors when the provider key was OpenRouter but the model id named another vendor. Fixes the 500 on `GET /sessions/{id}/messages` caused by non-JSON-serializable `set`/`frozenset` fields in ADK event payloads (EVO-1752), hardens `manage_conversation_labels` against destructive label replacement when the CRM label read fails transiently, and corrects `EvoAuthResponse` handling in chat token validation. CI now publishes `:pr-N`/`:sha-*` images per internal PR for the review environment (EVO-1998).
- **evo-ai-core-service-community** v1.0.0-rc6 — First functional release since rc3. Adds server-side advanced filtering to the Agents (EVO-1952) and Custom Tools + Custom MCP Servers (EVO-1953) list endpoints, mirroring the CRM advanced-filter payload with whitelisted attribute keys, fully parameterized clauses and array-aware tag matching, applied to both List and Count. The Custom Tool Test endpoint is now content-type-agnostic (EVO-1790): it no longer fails on non-JSON responses, reports the real status code, response time, headers and raw body, supports 7 HTTP methods and gains an SSRF guard. The community Docker image builds standalone again via `go.community.mod` (EVO-1998) after the enterprise `replace` in `go.mod` broke `go mod download`, and CI now publishes `:pr-N`/`:sha-*` images per internal PR for the review environment. Under the hood, the enterprise multi-tenancy extension points (tenant scope wiring, tenant_id stamping and cross-tenant leak guards — EVO-1621/1623/1624/1625) and the license-guardian boot hook (EVO-1989) land behind build tags and are strict no-ops in the community build. No new migrations or environment variables for community deployments.
- **evo-flow-community** v1.0.0-rc6 — 🆕 New family member: first tagged release of the event-tracking service (evo-flow + ClickHouse), added to the umbrella in EVO-1571 together with its local compose stack. Joins the family versioning at rc6; there is no prior rc tag for this service.
- **evo-bot-runtime** v1.0.0-rc6 — **(headline)** Restores agent conversation memory over a2a: the adapter now sends the conversation UUID as `contextId` and the contact UUID as `userId` (both extracted from `metadata.evoai_crm_data`, with numeric fallback for legacy callers), fixing the ADK session-key mismatch that produced "404/500 Session not found" and zero history on every turn — the agent answered each message from scratch. Also adds media-aware dispatch: media URLs are extracted from agent replies (Go mirror of the CRM's `MediaTypeDetector`) and delivered as structured attachments in a dedicated postback instead of raw text links. CI now publishes per-PR review images (`:pr-N` + `:sha`) for the review environment (EVO-1998), restricted to internal PRs.

### Non-family submodule pointers

- **evolution-api** — pointer moved from `7a55a2b` (rc5) to `dd552c7` (= remote develop). Not part of the family tag set; recorded here for traceability.
- **evo-nexus** (`fe15fd5`) and **evolution-go** (`706c9a4`) — unchanged since rc5.

### Notes for upgrading an existing PROD

- **CRM — 15 new migrations** (`20260608194533`..`20260701120000`): SendGrid channel tables (+ webhook status + email signature), `email_suppression` on contacts, message-template decoupling (+ `external_legacy_id`, template refs on inboxes, **data migration of legacy channel-coupled templates**), `stage_inactivity_executions`, `crm_forms`, `chat_pages`, `stage_movements` FK heal, `source` on messages and conversations, contact custom-attribute key backfill, and at-rest encryption of `EVOLUTION_HUB_API_KEY`. Run `db:migrate` on upgrade. On large databases, the legacy-template data migration and the custom-attribute backfill can take a while — plan a window.
- ⚠️ **CRM — storage default changed**: `ACTIVE_STORAGE_SERVICE` default in `.env.example` moved from `s3_compatible` to `local`. Installations that relied on the old default **must set `ACTIVE_STORAGE_SERVICE=s3_compatible` explicitly** or media handling silently switches to local disk.
- **CRM — new env `ATTACHMENT_DELIVERY`** (default `proxy`): the app now serves media bytes through `BACKEND_URL`, so the storage endpoint can stay private. Set `redirect` to keep the legacy behavior of redirecting to the storage service (which must then be reachable by browsers and sibling containers).
- **CRM — new optional env `ACTIVE_STORAGE_URL`**: host override for media URLs handed to sibling containers (Evolution API / Evolution Go) and for DiskService in `redirect` mode.
- ⚠️ **CRM — ActiveRecord Encryption keys required before migrating**: migration `20260701120000` encrypts `EVOLUTION_HUB_API_KEY` at rest and fails if the ActiveRecord Encryption keys are not configured in the target environment.
- **CRM — new flag `HUB_ALLOW_EXISTING_CHANNELS`** to disable the Hub "use existing channel" flow (cross-tenant leak mitigation).
- ⚠️ **Processor — RBAC gate (EVO-1956)**: after the upgrade, integrations routes require `integrations.*` permissions. Clients/roles that relied on the previously open access will start receiving 403 — verify the CRM roles grant the expected permissions before rollout.
- **auth-service — 4 new migrations** (`20260622120000`..`20260626130000`): RBAC operational/admin split, `crm_forms`/`chat_pages` resources, `conversations.import`, revoke of admin Settings from the agent role. Run `db:migrate` on upgrade — the CRM/frontend rc6 features (Lead Capture, conversations import) fail authorization without them. New optional env `COOKIE_DOMAIN` overrides the `public_suffix`-computed refresh-cookie domain (EVO-1964).
- **auth-service — poisoned `:latest` remediation (EVO-1404)**: operators who pulled `:latest` between 2026-05-27 and 2026-06-12 12:00 UTC got a desynced image (digest `sha256:4c08dca7…`) — re-pull is mandatory; expected digest is `sha256:7bed488c…`.
- **auth-service — email confirmation & GitHub OAuth**: email confirmation only fires when `SMTP_*` is configured; GitHub OAuth uses `GITHUB_OAUTH_*` resolved at runtime via `runtime_configs` (credentials can be rotated without restart).
- **frontend — new env `VITE_EVOFLOW_API_URL`** (EVO-1906): placeholder in the Dockerfile substituted by `docker-entrypoint.sh`. Deployments using EvoFlow must set it in the compose/stack; without it the placeholder remains in the bundle. The entrypoint also patches the CSP at runtime (plain-http media/API origins for non-TLS installs, EVO-1961) — compare the served CSP against the container, not the repo.
- **core-service**: no new migrations (migration 000016 was added and dropped within the same PR; the sequence still ends at 000015) and no new env vars for community deployments. If you build the image outside the official Dockerfile, build with `go build -modfile=go.community.mod ./cmd/api`.
- **bot-runtime — deploy order**: deploy the bot-runtime rc6 **after** the CRM rc6, which processes the new `Attachments` field of the postback (the CRM keeps its own detection as fallback, so a version skew degrades gracefully without losing media). No migrations, no new env vars — the upgrade is just the image tag.

### Repository housekeeping

- `evo-assistant` added to `.gitignore`.
- Footer links: this release adds the previously missing `[v1.0.0-rc5]` compare link and repoints `[Unreleased]` (it had been left at `v1.0.0-rc4...HEAD` since rc5).

## [v1.0.0-rc5] - 2026-05-27

Hardening release focused on **fresh-install reliability**. The previous rc4 image set, when deployed against an empty Postgres database with all services starting in parallel, hit a race where `evo-ai-processor-community` (Python/SQLAlchemy) created a foreign-key stub `users(id integer)` table before `evo-auth-service-community` (Rails) ran its `InitSchema` migration. The auth service then silently skipped its own `create_table :users` via `if_not_exists: true`, leaving authentication permanently broken (every call ended in `PG::UndefinedTable: relation "oauth_access_tokens" does not exist`, surfaced as 503 cascades in the CRM).

This release also closes a second-order issue introduced in rc4: the `Licensing::SetupGate` middleware was returning `503 SETUP_REQUIRED` for every non-bypass route whenever the licensing server was unreachable on first boot — bricking the CRM API even after the auth fixes. SetupGate now never blocks requests; licensing remains as observability only.

On the feature side, **EvoFlow expansion**: the CRM ships a `contact_events` backfill worker (ports historical Message activity + ReportingEvent rows into evo-flow's ClickHouse via `/events/batch`), the Ruby mirror of the EvoFlow event schema with `SchemaValidator`, and five new flow node types backed by shared `ActionService` handlers. The frontend ships a shared `EventSelector` + `EventPropertiesForm` consuming the event manifest, redesigned `NotificationItem`, and accessibility/i18n polish.

### Submodules updated

- **evo-auth-service-community** v1.0.0-rc5 — **(headline)** Three fresh-install fixes: drop foreign-key stub `users` table before `InitSchema` runs so the canonical schema is recreated; `Licensing::SetupGate` becomes observability and never blocks requests; Sidekiq now processes the `licensing` queue so `SetupJob` and `HeartbeatJob` can run. Also adds AuthBridge 1.1.0 extension point (`find_user_by_email` + `sign_in_request`) and minor fixes (`add_fk_if_missing` type-aware, ActiveRecord migration version compatibility).
- **evo-ai-crm-community** v1.0.0-rc5 — EvoFlow `contact_events` backfill worker (EVO-1243), Ruby mirror of EvoFlow event schema with SchemaValidator (EVO-1261), five new flow node types backed by shared ActionService handlers (EVO-1262), proxy `/contacts/:id/events` with enrich, notifications scope refinements (EVO-1419), Evolution Hub link-inbox-to-existing-channel feature, and an EvoFlow schema sanity fix that crashed the boot of the rc4 develop branch (missing `DEFINITIONS` entries for 5 conversation events).
- **evo-ai-frontend-community** v1.0.0-rc5 — Shared `EventSelector` + `EventPropertiesForm` consuming event manifest (EVO-1261), redesigned `NotificationItem`, Evolution Hub link-inbox-to-existing-channel, accessibility fix on `ConditionalNode` empty-state hint for WCAG AA (EVO-1454), inert floating-panel wrapper retirement (EVO-1421), i18n Spanish accent fix, locale-aware relative time via date-fns.
- **evo-ai-processor-community** v1.0.0-rc5 — Stops creating stub `users` table on `metadata.create_all` (the processor's contribution to the fresh-install race that broke auth); GitHub URL rename from `EvolutionAPI` to `evolution-foundation` in docs.
- **evo-ai-core-service-community** v1.0.0-rc5 — No code changes; version bump to keep the CRM Community family aligned.
- **evo-bot-runtime** v1.0.0-rc5 — Catch-up release. Service skipped `v1.0.0-rc4` (no functional changes warranted a tag then); `v1.0.0-rc5` realigns the bot-runtime image with the rest of the family. Go binary identical to `v1.0.0-rc3`.

### Notes for upgrading an existing PROD

- **Fresh installs (rc5 against an empty database)**: the multimport-class race condition is resolved. No manual intervention required — the processor no longer creates a `users` stub, and the auth service will drop any pre-existing stub before recreating the canonical schema.
- **Upgrading an installation broken by the rc4 race condition**: drop the cached stub `users` table (`DROP TABLE users CASCADE` on the shared Postgres) before pulling rc5, then redeploy. The auth service will recreate users with the correct schema on first boot.
- **Licensing**: `Licensing::SetupGate` no longer returns `503 SETUP_REQUIRED` on any endpoint, including when the licensing server is unreachable. Existing installs with `runtime_configs.api_key` already persisted are unaffected (heartbeat continues normally). Installs where the licensing-server call failed on first boot will now see all endpoints respond correctly while the background `HeartbeatJob` retries activation.
- **CRM**: an EvoFlow schema validation that runs at boot time (added in the rc4 develop branch) was missing entries for 5 conversation events. The rc5 image registers them. No upgrade action required.
- **CRM**: includes a `BackfillContactEventsWorker` (Sidekiq, queue `:integrations`, retry: 2) for historical `Message` activity and `ReportingEvent` rows. It is **dry-run by default** and only runs when invoked via `bundle exec rake evo_flow:backfill[<contact_id>]`. No automatic backfill.
- **Sidekiq (auth)**: the `licensing` queue was added to `config/sidekiq.yml`. If your deployment uses a custom `sidekiq.yml` (overridden via volume mount), make sure to include `- licensing` in the queues list.
- **bot-runtime**: the image tag jumps from `v1.0.0-rc3` directly to `v1.0.0-rc5`. The Docker image is the same content as rc3 — only the tag is new. Pull `evoapicloud/evo-bot-runtime:1.0.0-rc5` to keep the compose file consistent across the family.

### Repository housekeeping

- The processor history was reorganized between rc4 and rc5 (rebase on `develop`), so the GitHub compare link for the processor between the two tags shows more commits than there are functional changes. The image content is correct.

## [v1.0.0-rc4] - 2026-05-25

Release with two main themes: **(1) Evolution Hub** as an optional proxy for Meta channels, exposed end-to-end (admin configuration in the frontend, webhook receiver and inbox builder in the CRM), and **(2) Typebot interactive buttons** across processor / CRM / frontend / widget. Also rolls up MFA hardening in the auth-service (plaintext backup-code remediation post-EVO-991, session cache invalidation after re-setup), licensing fail-open, runtime storage provider, several CRM fixes (single-account assumptions, interactive-message hardening, macro execution status persistence), frontend chat fixes (sidebar scroll, conversation count, loadMore race), and a menu cleanup that hides in-development entries.

### Submodules updated

- **evo-auth-service-community** v1.0.0-rc4 — MFA hardening (EVO-991 plaintext backup-code invalidation migration + EVO-1104 session cache invalidation), licensing fail-open during outages, runtime storage provider (EVO-1050), onboarding survey push.
- **evo-ai-crm-community** v1.0.0-rc4 — Evolution Hub as optional proxy for Meta channels (webhook receiver + InboxBuilder + lifecycle), Typebot interactive buttons, EVO-1088 macro execution status + webhook failure surfacing, EVO-1372 interactive-message hardening, legacy single-account assumption fixes, internal events module groundwork (not user-facing).
- **evo-ai-frontend-community** v1.0.0-rc4 — Evolution Hub admin page + `HubConnectButton`, Typebot interactive buttons in chat and widget, EVO-1088 real macro execution result in UI, `NodeConfigModal` + `JourneyEditorHeader` + `useFlowEditorStore` shared components (groundwork for upcoming features, not user-facing), chat fixes (sidebar scroll, conversation count, loadMore race), menu cleanup hiding in-development entries.
- **evo-ai-processor-community** v1.0.0-rc4 — Typebot interactive button rendering paired with CRM/frontend.
- **evo-ai-core-service-community** v1.0.0-rc4 — no functional changes; tag issued to keep the CRM Community family aligned on a single release-candidate version.

### Notes for upgrading an existing PROD

- **auth-service**: the plaintext backup-code invalidation migration runs `UPDATE` with a row-level lock on the `users` table. For databases with more than 100k users, schedule a short maintenance window. Users that had MFA enabled before this release will be prompted to set up TOTP again on next login (precaution post-EVO-991).
- **crm**: includes a legacy schema cleanup migration. Run `db:migrate` on upgrade. No impact on production data — the removed tables were not in use.
- **frontend**: in-development menu entries are hidden in this release. Routes remain in the app; only sidebar visibility was adjusted. No operator or end-user action required.
- **processor**: no operational changes; image rebuilt with the Typebot rendering update.
- **core-service**: no operational changes; image rebuilt to stay aligned with the family.

## [v1.0.0-rc3] - 2026-05-17

Stabilization release following `v1.0.0-rc2` (2026-05-05). A ~12-day window with ~16 commits in the super-repo and ~165 commits/PRs across the submodules. Predominant focus on **bug fixes** for production issues identified after rc2 — Evolution Go messaging, outbound media, public endpoint hardening, 2FA, RBAC, IDOR scoping, secret filtering in logs — combined with the technical foundation of the open-core (Extension Points across all services + Plugin Host Runtime in the frontend) and two cross-stack features: products catalog and template bundles export/import.

### Highlights

- 🐛 **Massive bug fix release** — 6 main hardening fronts: Evolution Go payload parity (buttons/lists EVO-1115), outbound media delivery (EVO-1151), Notificame verify hardening (EVO-986), bulk actions with IDOR scoping (EVO-1084), secret filtering in Rails logs (EVO-1111), 2FA backup codes hash+500 (EVO-991).
- 🧩 **Complete open-core foundation**: all 5 submodules now declare `EXTENSION_POINTS.md` + no-op modules. The frontend gained a **Plugin Host Runtime** (EVO-1379) that loads external plugins without forking. The auth-service gained `LoginGate` and `TokenClaims` as strict extension points. The CRM gained a CI guard-rail (EVO-1287) that prevents silent contract changes.
- 📦 **Products catalog** — products model with variants, attachable to agents, pipeline integration for sales. Native tools in the processor (`link_product_to_pipeline_item`) and catalog injection into the agent context.
- 📤 **Template bundles export/import (EVO-1116)** — packaging of configuration (inboxes, agents, automation rules, canned responses, templates) into a ZIP portable across installations. Dedicated RBAC permission (`template_bundles.manage`), export wizard in the frontend, i18n pt/es/fr/it.
- 🛡️ **Complete Roles & Permissions UI (EVO-1061)** — custom roles administration screen with full CRUD, `account_owner` scoping, and guard against privilege escalation via delegation of permissions not held.
- 🔌 **Knowledge Nexus integration** — agents can search Nexus spaces directly from the prompt (`knowledge_nexus_search` tool in the processor + space picker in the frontend Agent Builder + proxy endpoint in the core-service).
- 🤖 **Automation rules — consolidation**: `attribute_changed` operator on labels (EVO-1058), `conversation_resolved` / `conversation_status_changed` listeners (EVO-1057), `move_to_pipeline` cross-pipeline action, 5s window dedup, logs panel in the frontend.

### Added

- **Plugin Host Runtime in the frontend (EVO-1379)** — loads external plugins at runtime; foundation for the Enterprise edition to inject features without forking.
- **`EXTENSION_POINTS.md` in all 5 submodules** — versioned public contract for extension points. Auth: `LoginGate` + `TokenClaims`. CRM: 4 hooks + `lib/evo_extension_points/` no-op + CI guard-rail (EVO-1287). Frontend: 4 declared categories (EVO-1284/1378) with Plugin Host Runtime in v2.1.0 (EVO-1387). Core-service: `pkg/evoextensions` with 3 no-op interfaces (EVO-1285). Processor: hooks document (EVO-1376).
- **Products catalog (CRM + frontend + processor)** — model with variants, attachable to agents, sales panel in the pipeline, injection into the agent context, `link_product_to_pipeline_item` tool, `products.*` RBAC permissions.
- **Template bundles export/import (EVO-1116)** — cross-stack feature (CRM + frontend + auth RBAC) to package installation configuration into a ZIP. `template_bundles` resource declared in auth, endpoint in the CRM, export wizard in the frontend with i18n.
- **Roles & Permissions admin UI (EVO-1061)** — full roles management screen in the frontend + CRUD API in the auth-service + `account_owner`/`super_admin` boundary guards + regression spec.
- **Knowledge Nexus integration** — native `knowledge_nexus_search` tool in the processor, space picker in the Agent Builder, proxy endpoint in the core-service.
- **Native tools in the processor LLM agent** — `knowledge_nexus_search`, `manage_conversation_labels`, `link_product_to_pipeline_item`.
- **Automation rules** — `attribute_changed` operator with From/To pickers (EVO-1058), `conversation_resolved` and `conversation_status_changed` listeners (EVO-1057), `move_to_pipeline` action (cross-pipeline), logs panel in the frontend, action service with `send_canned_response` and `send_template`.
- **Bulk actions** — bulk resolve of conversations via checkbox (EVO-1011), response with per-item `success_ids` / `failed_ids`.
- **Pipelines — `move_to_pipeline` action** — automation moves conversation across pipelines preserving id, with 5s window dedup.
- **EVO-1051** — `DELETE` endpoint to clear admin config by type (CRM) + "Clear Configuration" button in Admin Settings (frontend).
- **EVO-1189** — Delete contact action in the frontend.
- **EVO-990** — Pipeline actions available in the 3-dot menu and context menu (right-click).
- **EVO-988** — Contact phone number visible in the conversation list and chat header.
- **EVO-1146 — i18n** — multiple missing keys added across 6 frontend locales; pt/es/fr/it locales added for template bundles.
- **Regression specs** — `pipeline_item` auto-assign-and-move (EVO-1080), Notificame verify (EVO-986), contact with attachments (EVO-973), macro webhooks (EVO-1041), `account_owner`/`super_admin` boundary (EVO-1060), `agent` role permission set (EVO-1060).

### Changed

- **EVO-1049 — SMTP/BMS/Resend applied at runtime in the auth-service** — operator can swap these configs via UI without restarting the container. The frontend dropped the workaround banner (rc2) that asked for a restart.
- **EVO-1113 — Consolidation of Evolution credential resolution in the CRM** — a single concern (`EvolutionConcern`) centralizes per-field fallback for `api_url`/`admin_token`. Reduces bug surface between Evolution API and Evolution Go.
- **EVO-1147 — Provider config polling in the frontend** — Page Visibility API integrated, no polling in background tabs; `provider_config` removed from deps.
- **EVO-1085 — WebSocket reconnection** — active reconnection with success toast + background backoff.
- **EVO-1131 — Large file upload** — skip of fetch+blob, limit raised to 100MB.
- **EVO-1044 — Per-field GlobalConfig fallback detection** — Connection Settings banner now detects field by field.
- **EVO-976 — Avatar storage** (#80, umbrella) — shared volumes, `AUTH_SERVICE_URL` documented, storage docs updated.
- **`EVOLUTION_OPERATOR_EMAIL`** documented in `.env.example` (licensing).
- **Docs / branding** — entire stack standardized to Evolution Foundation 2026 (README, LICENSE, NOTICE, TRADEMARKS); GitHub URLs migrated from `EvolutionAPI` to `evolution-foundation`.
- **Docker tag convention** — fixed in `release.yml` and the umbrella README (no `v` prefix in Docker tags).
- **CI** — workflows now run on PRs against `develop` (not only `main`); Linear/CRM packages with PR link fetched from Linear comments in the `code-review` skill.

### Fixed

#### Messaging — Evolution Go / Evolution API
- **EVO-1115** — buttons/lists payload corrected to the Evolution Go format (parity with Evolution API). Interactive messages were arriving malformed.
- **EVO-1151** — outbound media delivery failure on both providers (Evolution API and Evolution Go).
- **Duplicated messages in the Evolution Go incoming handler** — dedup at the entry point.
- **`api_url` / `admin_token` fallback** — falls back to `GlobalConfig` when the inbox config is empty.

#### Stability / REST API
- **2FA backup codes** (EVO-991, auth) — 500 NoMethodError + plaintext hash in the database. Fixed with BCrypt + null field handling.
- **EVO-1063 — Structured 422 password validation** (auth + frontend) — response with machine-readable codes consumed by an inline checklist in the user creation form.
- **EVO-1046 — `setupRequired=false` default** when `/setup/status` errors out (frontend) — previously a 5xx on setup status blocked the entire app.
- **EVO-1107 — Configuration tab blank/slow load** — skeleton + polling fixed.
- **EVO-1048 — Collapsed sidebar** — submenu flyout and tooltip appear when the sidebar is collapsed.
- **EVO-1145 — Conversation match in reducers** — now matches by `id || uuid`.
- **EVO-1078 / 1054 / 1062 / 1056** — multiple chat and auth bugs resolved in a batch.

#### Webhooks / Notificame
- **EVO-986 — Notificame verify endpoint hardening** — mandatory auth, payload validation, no error leakage; regression spec.
- **EVO-1041 — Macro webhook delivery failures** — failures are now surfaced; re-raise restricted to `:macro_webhook` to avoid retry storms.
- **EVO-1130 — Attachment fallback_title** — prefers `content[:fileName]`.

#### Automation / Pipeline
- **`labels` condition** — `EXISTS` subquery (independent, NULL-safe), resolves UUIDs to titles, matches label on conversation OR contact.
- **`message_type` filter** — accepts numeric values.
- **`apply_label` action** — resolves UUIDs to titles before tagging; opens label picker in the frontend.
- **`pipeline_stage_updated`** — 5s window dedup by `(rule, pipeline_item, stage)`.
- **Cross-pipeline stage movement** — correct bypass of `same-pipeline` validation.
- **Build break** — `MessageTemplateVariable` defined locally.
- **Menu** — duplicate automation item removed.
- **EVO-1018 — Group contacts** — distinguishes WhatsApp group contacts from real contacts (CRM + frontend).
- **EVO-998** — orphan contact event files and dead i18n removed.

#### RBAC
- **EVO-1060 — `agent` role** — `pipelines.read` backfilled, `pipelines.update` removed (it would have unlocked destructive endpoints).

#### Media (EVO-999)
- **HIGH review findings** applied: video file_type fallback, attachment fallback_title, force-download via fetch+blob covered on all paths.

#### Other
- **DB asyncpg** (processor) — `sslmode` translated to `ssl` (native driver parameter).
- **Docker bundler** (CRM) — version pinned at install.

### Security

- **EVO-1111 — Secret filtering in Rails logs** (CRM) — sensitive fields (password, token, api_key) filtered before logging.
- **EVO-1084 — IDOR scope in `BulkActionsJob`** (CRM) — account scoping applied; previously it was possible to manipulate cross-tenant resources with a valid ID.
- **EVO-1061 — Privilege escalation via delegation** (auth) — `account_owner` can no longer delegate permissions they do not themselves hold.
- **EVO-986 — Notificame verify** (CRM) — mandatory auth + no error leakage.
- **2FA backup codes** (auth) — codes hashed with BCrypt; previously stored in plaintext in the database.

### Notes for upgrading an existing PROD

- ✅ **`agent` role RBAC changes** are activated automatically via `db:migrate` (EVO-1060) — no reseed required.
- ✅ **SMTP/BMS/Resend runtime** (EVO-1049) — applied automatically after upgrading the auth-service. Operator can swap configs without restart.
- ✅ **Log secret filtering** — activates automatically after upgrading the CRM. Old logs are not affected (only new entries).
- ⚠️ **2FA backup codes** — starting in rc3, codes are stored hashed with BCrypt. Codes generated before rc3 remained in plaintext in the database; if the database history was accessible to anyone outside the installation operator, regeneration via UI is recommended.
- 📝 **`EXTENSION_POINTS.md`** — public contract only; no migration action required. Reactive for the Enterprise edition that injects the implementations.
- 📝 **Per-submodule CHANGELOG** has the full technical detail — this section is the umbrella summary.

## [v1.0.0-rc2] - 2026-05-05

Stabilization release following `v1.0.0-rc1` (2026-04-24). A ~3-week window concentrating ~40 orchestration commits in the super-repo and ~70 PRs across the submodules. Focus on four fronts:

1. **Docker / deterministic setup** — `make setup` on a fresh install works without race conditions between services
2. **Cloud / WhatsApp media** — private S3 buckets, PTT-compatible audio recording, inline video rendering
3. **`super_admin` RBAC** — installation operator separated from `account_owner`, with automatic upgrade on existing PROD
4. **API stability** — elimination of `500 Internal Server Error` on REST endpoints, end-to-end Evolution Go flow fixed

### Highlights

- 🎙️ **WhatsApp Cloud audio finally works in production**: after 4 attempts with FFmpeg WASM (all blocked by SharedArrayBuffer / COOP+COEP requirements / corrupted worker on npm), we pivoted to `opus-recorder@8.0.5` — direct PTT-compatible OGG/Opus recording in the browser, no reencode, no server-side latency.
- 🎬 **Video in chat appears as a player**, no longer as a "Download file" attachment.
- 🗄️ **Media in private buckets works**: signed URLs applied on both the Evolution API and Evolution Go providers.
- 🔐 **New `super_admin` role**: the installation operator has exclusive access to the `/settings/admin` panel (SMTP, Storage, Auth Providers, OpenAI, Channels, Inbound Email). An automatic migration promotes the bootstrap user on existing installations and revokes their active tokens to force re-login with the new role.
- 🧪 **E2E Playwright** validating the audio recording pipeline with a fake microphone — feedback cycle dropped from a 10-minute deploy to 5s locally.
- 🛠️ **Deterministic `make setup`**: full idempotency in Rails service migrations resolves the race condition with `evo-bot-runtime` Go core on `users` table creation.

### Added

- **`super_admin` role** in `evo-auth-service-community` — installation-level operator. Holds all `account_owner` permissions plus `installation_configs.manage` (access to the `/settings/admin` panel). Automatically assigned to the setup wizard user. Existing PROD receives it via `db:migrate` (promotes `User.order(:created_at).first`).
- **`Role::ADMIN_ROLE_KEYS` constant** in the CRM — centralizes `%w[account_owner super_admin]`, adopted by admin mailers and finders. Previously the list was hardcoded in four places and excluded `super_admin`, causing inconsistent behavior in admin bypasses.
- **`user_tours` table** in the auth-service — persistence of onboarding tour state per user.
- **Audio recording E2E suite** in the frontend — Playwright + Chromium with fake media stream. `e2e/audio-recording.spec.ts` validates that `recordPttOgg` produces an `audio/ogg` blob with `OggS` magic bytes in a real browser environment.
- **`MessageVideo` component** in the frontend — inline render with `<video controls preload="metadata" playsInline>`, fallback to a download tile when codec is not supported.
- **"Automation" tab in the Edit Stage Modal** (EVO-989, frontend) + **`Pipelines::StageAutomationService`** (EVO-989, CRM) — `trigger → action` rules per pipeline stage.

### Changed

- **WhatsApp Cloud — audio recording: FFmpeg WASM → `opus-recorder`**. Full saga documented in `evo-ai-frontend-community/CHANGELOG.md`. Summary: the Cloud API requires OGG/Opus PTT; the first solution tried converting webm → ogg in the browser via FFmpeg WASM, but the 4 versions tested failed for distinct architectural reasons (SharedArrayBuffer, 0-byte worker on npm, unconditional worker fetch in the wrapper). Replaced by `opus-recorder@8.0.5`, which captures raw PCM and encodes directly to OGG/Opus via `libopusenc` — no reencode, no cross-origin isolation requirements, no server round-trip.
- **Media in private S3 buckets** (CRM): `generate_direct_s3_url` replaced by `presigned_url` in `whatsapp/providers/evolution_go_service.rb` and `whatsapp/providers/evolution_service.rb`. Previously the direct public URL returned 404 when the bucket was private (Cloudflare R2, S3 with private ACL).
- **Conversation list — `pipeline_items` preload**: `ConversationFinder#build_conversations_query` kept a minimal preload, so the pipeline chip in the listing only appeared after manual tagging. Added `pipeline_items: [:pipeline, :pipeline_stage]` to the preload.
- **Admin Settings UX in the frontend**: "Social Login" renamed to "Authentication Providers" (reflecting generic OAuth, not only social networks), Twitter tab hidden (deprecated provider), "configuration via env" warning banners on SMTP/Storage to make it clear that UI changes do not persist in PROD.
- **CI**: `validate-compose` and `lint-dockerfiles` workflows now run on PRs against `develop` (not only `main`). (#59)
- **Submodules**: coordinated bumps over the rc2 window:
  - `evo-ai-crm-community`: 19 PRs/commits (automation rules EVO-989, navigation EVO-1007, idempotent migrations, end-to-end EvoGo fixes, contact import, super_admin RBAC, signed S3 URLs, etc.)
  - `evo-ai-frontend-community`: 11+ PRs/commits (opus-recorder, inline video, automation UI, role select, team members, brand colors, admin settings UX, e2e Playwright, etc.)
  - `evo-auth-service-community`: super_admin role + automatic upgrade migration with token revocation, password forwarding fix on user creation, full idempotency in init_schema, user_tours table
  - `evo-ai-processor-community`: `python -m` for alembic/uvicorn + idempotency
  - Other submodules: CI adjustments

### Fixed

#### Setup / Docker / Orchestration
- **`Makefile` — database setup sequence**: `make setup` now creates the DB in the CRM, runs `db:schema:load` (loads the master schema, including all tables the auth-service uses), marks auth migrations as applied via `rails runner` with deterministic `.sort` and a specific `rescue ActiveRecord::RecordNotUnique`, and only then runs `db:seed` on the CRM followed by the auth. Without this, `make setup` on a fresh install failed with `PG::UndefinedTable: roles`. (cherry-pick from PR #69 — authorship by @andersonlemesc preserved)
- **Fully idempotent `init_schema` in the auth-service** — a race condition between the auth-service setup and `evo-bot-runtime` Go core (which creates a minimal `users` table on boot) caused `init_schema` to fail with `PG::DuplicateTable` when Go won the race. Rewritten with `if_not_exists: true` on all `create_table`/`add_index` and an `add_fk_if_missing` helper for foreign keys.
- **Docker — `auth_storage`**: named volume replaced with a bind mount, fixing `permission denied` when writing files in the authentication service. Bind mount also extended to `sidekiq` with a defensive `mkdir` in the entrypoint. (#65, #72)
- **Docker — Alpine compat**: swapped `bash -c` for `sh -c` in internal scripts for compatibility with Alpine images. (#31)
- **Docker — healthcheck**: fixed the `evo-core` healthcheck path. (#26)
- **Env validation (EVO-985)**: block `BACKEND_URL` / `FRONTEND_URL` pointing to `localhost` in production — fail fast on boot instead of serving invalid URLs to external clients. (#75)
- **Submodules**: retargeting of orphan SHAs to public branches (`develop` / `main`). Eliminated CI checkout errors caused by lost SHAs.

#### Media / Chat
- **WhatsApp Cloud audio did not reach the recipient** — Meta rejects `audio/webm` as a voice message. Solved by the migration to `opus-recorder` (see Changed above).
- **Video appeared as a "Download file" attachment** — `MessageBubble` was falling into the generic fallback. New `MessageVideo` with a native player.
- **Media in private bucket returned 404** — signed URLs applied on both providers (see Changed).

#### RBAC
- **`super_admin` ignored by CRM bypasses** — hardcoded lists of administrative roles filtered only `account_owner`. `User#administrator?` and `Role::ADMIN_ROLE_KEYS` consolidated recognition; without this, super_admin saw an empty conversation list, admin mailers did not arrive, etc.

#### API stability (CRM, from the `develop` cycle)
- **`PATCH /api/v1/pipelines/:id/pipeline_items/:id/update_custom_fields`** raised `NoMethodError` (before_action skipping the action). (#32)
- **`POST /api/v1/contacts/:id/companies`** raised `NoMethodError` in `must_belong_to_same_account`. (#34)
- **`POST/DELETE /api/v1/contacts/:id/companies`** returned 500 on business rule violation (incompatible kwargs in `error_response`). (#35)
- **`/api/v1/agents/*`** returned 500/Unauthorized (request.headers not forwarded, current_user used as the wrong positional argument). (#33)
- **`GET /api/v1/oauth/applications`** returned an array instead of the standard envelope, breaking the OAuth Apps screen. (#36)
- **EVO-1000** — `POST /api/v1/team_members` returned 401 for every valid UUID (`map(&:to_i)` on a UUID PK). (#24)

#### Evolution Go (EvoGo) — end-to-end WhatsApp flow (#22)
- Conversation routing by LID (no more duplicated conversations on each outgoing send)
- Correct sender type, contact lookup via inbox joins, reopening of pending conversations
- Media saved without a file (3 issues: ActiveStorage commit in Sidekiq, nested `mediaUrl`, inline base64 for EvoGo without S3)
- Audio without waveform (duplicate definitions of `configure_audio_metadata`)
- ActionCable broadcast on empty token

#### Auth-service
- **`POST /api/v1/users` returned 500 without `role`** — fallback to `agent` instead of `Role.find_by!(key: nil)`. (#9)
- **Login always 401 for users created via UI** — `password` was not forwarded to `AgentBuilder.new`, so each agent was born with a random Argon2 hash nobody knew. (commit `917c366`)
- **`add_message_template_permissions_to_account_owner` migration** failed on fresh install with `PG::UndefinedTable: roles` due to timestamp ordering. Added a `table_exists?(:roles)` guard.
- **EVO-971**: `/setup/status` gate now considers bootstrap, not only licensing. (#8)
- **EVO-967**: invited agents are auto-confirmed; role lookup tolerates a missing role without a 500. (#3)

### Notes for upgrading an existing PROD

- ⚠️ **`db:migrate` of `evo-auth-service-community` revokes active tokens of the bootstrap operator** when promoting them to `super_admin`. The operator will be forced to log out/in once on the first request after the upgrade. This is expected and necessary for the JWT to reflect the new role.
- ⚠️ **Other `account_owner` users lose access to the `/settings/admin` panel** — intentional behavior (the panel is reserved for installation operation, not account management). If you created multiple `account_owner` users in rc1 and want more than one of them to have admin access, you will need to promote them manually to the new role via console (`User.find(...).user_roles.create!(role: Role.find_by!(key: 'super_admin'))`).
- ✅ **Media in private S3 bucket**: the signed URL fix is purely backend and activates automatically after upgrading the CRM. No migration action required.
- ✅ **WhatsApp Cloud audio**: active automatically after the frontend upgrade. A hard browser refresh is required to invalidate the old bundle.
- 📝 **Per-submodule CHANGELOG** has the full technical detail for each item — this section is the umbrella summary.

## [v1.0.0-rc1] - 2026-04-24

### Added

- First public release candidate of the **CRM Community**.
- Initial composition of 7 submodules via Docker Compose:
  - `evo-ai-crm-community`
  - `evo-ai-frontend-community`
  - `evo-ai-core-service-community`
  - `evo-ai-processor-community`
  - `evo-auth-service-community`
  - `evo-bot-runtime`
  - `evolution-api`, `evolution-go` (WhatsApp providers)
- `Makefile` with setup, seed, and dashboard targets.
- Bootstrap scripts (`setup.sh`) and `docker-compose` examples (dev, prod-test, swarm).
- `.env` templates and `Apache 2.0` license.

---

[Unreleased]: https://github.com/evolution-foundation/evo-crm-community/compare/v1.0.0...HEAD
[v1.0.0]: https://github.com/evolution-foundation/evo-crm-community/compare/v1.0.0-rc7...v1.0.0
[v1.0.0-rc7]: https://github.com/evolution-foundation/evo-crm-community/compare/v1.0.0-rc6...v1.0.0-rc7
[v1.0.0-rc6]: https://github.com/evolution-foundation/evo-crm-community/compare/v1.0.0-rc5...v1.0.0-rc6
[v1.0.0-rc5]: https://github.com/evolution-foundation/evo-crm-community/compare/v1.0.0-rc4...v1.0.0-rc5
[v1.0.0-rc4]: https://github.com/evolution-foundation/evo-crm-community/compare/v1.0.0-rc3...v1.0.0-rc4
[v1.0.0-rc3]: https://github.com/evolution-foundation/evo-crm-community/compare/v1.0.0-rc2...v1.0.0-rc3
[v1.0.0-rc2]: https://github.com/evolution-foundation/evo-crm-community/compare/v1.0.0-rc1...v1.0.0-rc2
[v1.0.0-rc1]: https://github.com/evolution-foundation/evo-crm-community/releases/tag/v1.0.0-rc1
